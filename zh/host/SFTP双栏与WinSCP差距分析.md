# SFTP 双栏与 WinSCP 差距分析

> 编写日期：2026-07-22　　基准代码：`tmds-ssh` 分支
>
> 用途：**决策清单**。逐项列出 VelaShell 双栏 SFTP 与 WinSCP 的差距，供后续挑选要实现的功能。
> 每一项都标注了当前状态与实现代价，未核实的一律标注"未核实"，不做推测。

---

## 一、先看结论

VelaShell 目前是一个**做得相当扎实的单向文件传输器**，但还不是一个**双栏文件管理器**。

传输管道本身的质量明显高于同类小工具：递归上传/下载规划、四种冲突策略、断点续传（含起点核实）、
限速、并发、进度浮窗、传输日志、取消语义、`LocalPathSafety` 路径穿越校验。
远端栏的表格（7 列、可排序、可隐藏、可拖宽、双击自适应）也超出预期。

差距集中在三个层面，**处理方式完全不同，不要混在一起排期**：

| 类别 | 含义 | 用户感知 | 建议 |
|---|---|---|---|
| **A. 接线债** | 代码写好了，但没接到双栏这条路 | **像 bug** | 优先修，代价极低 |
| **B. 能力缺失** | 确实没做，但架构支持 | 功能少 | 按需求排期 |
| **C. 架构级缺失** | 连接口层都没预留 | 与 WinSCP 的本质差距 | 需要专门设计 |

---

## 二、A 类：接线债（建议优先处理）

这些**不是"没做"，是"做了但没接上"**。用户会当成 bug 报，修复成本却很低。

| # | 问题 | 位置 | 现象 |
|---|---|---|---|
| A1 | **外部编辑器在双栏里永远报"未配置"** | `SftpDocumentViewModel.cs:33-40` 未赋值 `GetDefaultEditorPath` | 用户在设置里配了编辑器也用不了。终端侧宿主有接（`MainWindowViewModel.cs:846`），双栏漏了 |
| A2 | **本地栏表头无法排序** | `LocalFilePaneView.axaml:128-138` 表头是纯 `TextBlock` | `SortCommand`（`LocalFilePaneViewModel.cs:49`）与 `ToggleSort:499` 完整可用，就是没绑 |
| A3 | **传输浮窗无法单条取消 / 重试 / 清空** | `FileTransferView.axaml` 只绑了 `CancelAllCommand`、`HidePanelCommand` | 三个命令（`FileTransferViewModel.cs:106-112`）没有对应控件。**且 `RetryTransfer:419` 本身只改状态不重发**，接上 UI 也是空动作 |
| A4 | **`TransferManager` 整套队列零调用点** | `Core/Sftp/TransferManager.cs` | 排队 + 并发 + 取消都实现了、DI 也注册了，但生产代码从不调用 `QueueTransferAsync`。浏览器自己用 `SemaphoreSlim` 跑 |
| A5 | **远端栏"上级目录"没有按钮** | `GoUpCommand`（`FileBrowserViewModel.cs:648`）无 axaml 绑定 | 只能双击 `..` 行。本地栏有按钮 |
| A6 | **双栏关掉远端栏后无法恢复** | `ToggleVisibilityCommand` → `IsVisible=false` | 该按钮是给终端宿主设计的（那边有重开路径），双栏里点了就只剩空白 |
| A7 | **双栏的传输设置是构造时快照** | `SftpDocumentViewModel.cs:36-37` | 改了设置对已打开的 SFTP 文档不生效；`OnSettingsSaved` 只遍历终端侧缓存 |
| A8 | **双栏不读/不写列显示与隐藏文件设置** | `SftpDocumentViewModel.cs:33-40` | 终端侧接了 `ShowHiddenFiles`、`ColumnVisibilityToggled`，双栏没接 |
| A9 | **`DownloadItemCommand` 无绑定** | `FileBrowserViewModel.cs:670` | 连带 `PickSavePathForDownload`（"下载单文件时另存为"）在 UI 上不可达 |
| A10 | **续传下载不套限速** | `SftpService.cs` 续传分支未包 `ThrottledStream` | 断点续传时带宽限制静默失效 |
| A11 | **传输日志把 Copy 记成 DOWNLOAD** | `TransferLogService.cs:40` 三元只区分 Upload/其它 | 日志类型不准 |
| A12 | **OS 拖文件到本地栏只刷新、不复制** | `LocalFilePaneView.axaml.cs:234-243` | 拖进去什么也没发生 |

> **重复定义**：`AppSettings.AutoResume`（:623）与 `ResumeEnabled`（:668）语义重叠，只有后者被消费。
> `TransferMaxRetries`（:671）、`AutoCleanTempFiles`（:674）代码注释自认"规划中，无运行时消费者"。

---

## 三、B 类：能力缺失（按 WinSCP 功能域对照）

### 3.1 浏览与导航

| WinSCP 能力 | VelaShell | 说明 |
|---|---|---|
| 可编辑路径输入框 | ❌ 无 | 两侧都只有面包屑，无法直接粘贴路径跳转。**高频刚需** |
| 前进 / 后退历史 | ❌ 无 | 两个 VM 都没有历史栈 |
| 书签 / 收藏夹 | ❌ 无 | WinSCP 用户依赖度很高 |
| 目录访问历史下拉 | ❌ 无 | |
| 面包屑 | ✅ 双侧已实现 | |
| 隐藏文件开关 | ⚠️ 仅远端 | 本地栏永远显示隐藏文件且无开关 |
| 本地盘符切换 | ✅ 已实现 | |

### 3.2 文件操作

| WinSCP 能力 | VelaShell | 说明 |
|---|---|---|
| 新建文件夹 / 文件 | ✅ 远端齐全 | 本地栏**只有新建文件夹，没有新建文件** |
| 重命名 | ✅ 双侧 | |
| 删除（递归 + 进度 + 取消） | ✅ 双侧 | 质量不错，远端还有加权进度。**SSH 会话上删目录走一条 `rm -rf` 快路径**（2026-09-20，`Transfer.UseRecursiveDeleteCommand`，默认开）：SFTP 递归的往返次数与文件数成正比，`rm -rf` 是一次往返；命令不可用或退非零码即回退 SFTP 递归。代价是这条路上没有逐条进度（进度条转不确定态），二次确认不受影响 |
| 移动 / 复制（远端→远端） | ⚠️ 要手敲绝对路径 | 无目录选择器、无拖放移动 |
| chmod | ⚠️ 仅单文件 | 有 9 格 rwx 矩阵 + 八进制同步，但**无批量、无递归应用、无 setuid/sticky 位** |
| **chown（改属主/属组）** | ❌ 无 | `ISftpService` 无接口，属性弹窗里 Owner/Group 只读 |
| 符号链接创建 / 识别 | ✅ 已实现（2026-09-12） | 链接行有专用图标与「→ 目标」提示，权限串首字符为 `l`，指向目录的链接可直接进入；右键「新建符号链接」。删除只删链接本身、复制得到链接（`cp -P`）、文件夹下载不跟进嵌套的目录链接。FTP 仅支持 `SITE SYMLINK` 的服务器可创建，插件协议不支持创建 |
| **修改远端时间戳** | ❌ 无 | 仅下载时保留时间戳；上传不回写 mtime |
| 本地文件属性 / 权限 | ❌ 无 | 本地栏右键只有 5 项 |

### 3.3 传输

| WinSCP 能力 | VelaShell | 说明 |
|---|---|---|
| 拖放（双栏互拖、OS→远端） | ✅ 已实现 | |
| **拖放到具体目标文件夹行** | ❌ 无 | 目前 Drop 只作用于"当前目录"，不解析落点 |
| **双击传输** | ❌ 语义不同 | 远端双击 = 下载并用 OS 程序打开；本地双击文件 = **什么都不做** |
| 冲突处理（覆盖/跳过/重命名/询问） | ✅ 四种齐全 | 质量很好 |
| 断点续传 | ✅ 已实现 | 含起点核实与安全回退窗口 |
| 限速 / 并发数 | ✅ 已实现 | 但见 A10 |
| **传输队列（可视化、可暂停、可调序）** | ❌ 见 A4 | WinSCP 的队列是核心体验 |
| **传输后校验（checksum）** | ❌ 无 | |
| 完成通知 | ✅ 已实现 | |

### 3.4 选择与批量

| WinSCP 能力 | VelaShell | 说明 |
|---|---|---|
| 多选 / 右键纳入选区 / 跨导航保持选区 | ✅ 已实现 | |
| **通配符选择（`*.log`）** | ❌ 无 | WinSCP 的 `Select Files` 是高频操作 |
| **反选** | ❌ 无 | |
| **递归选择** | ❌ 无 | |
| 全选 | ⚠️ 仅靠 ListBox 默认行为 | 无显式 `Ctrl+A` 绑定 |
| **键盘快捷键（F5/F2/Del/Enter）** | ❌ 全无 | 两个 View 都没有 `KeyBindings`，也没有 `KeyDown` 处理。**WinSCP 老用户会非常不适应** |

### 3.5 搜索与过滤

| WinSCP 能力 | VelaShell | 说明 |
|---|---|---|
| **当前目录过滤框** | ❌ 无 | 500 个文件的目录里找一个只能靠眼睛 |
| **远端递归搜索** | ❌ 无 | `ISftpService` 无任何 Find/Search API |

### 3.6 编辑与查看

| WinSCP 能力 | VelaShell | 说明 |
|---|---|---|
| 内置编辑器 + 保存自动回传 | ✅ 已实现（5MB 上限） | **2026-07-22 已补语法高亮**，见下节 |
| 外部编辑器 | ⚠️ 见 A1 | 逻辑完整，双栏没接线 |
| **文件预览面板** | ❌ 无 | |

### 3.7 视图

| WinSCP 能力 | VelaShell | 说明 |
|---|---|---|
| 远端按列排序 / 列显隐 / 列宽拖拽 | ✅ 已实现 | |
| 本地栏排序 | ⚠️ 见 A2 | |
| **列宽跨重启记忆** | ❌ 无 | 仅存在于 VM 实例 |
| **分栏比例记忆** | ❌ 无 | GridSplitter 可拖但不持久化 |
| **本地栏只有 3 列** | ❌ | 无权限/属主/属组/类型 |
| 图标视图 / 详细视图切换 | ❌ 无 | |

---

## 四、C 类：架构级缺失

这三项**连接口层都没有预留**（`ISftpService.cs` 全文 82 行无相关签名），需要专门设计。
（C1 已于 2026-09-12 完成，见第七节。）

### C1. 同步与目录比较 —— ✅ 已实现（2026-09-12）

WinSCP 的三大同步能力现已具备，适用于 SFTP、FTP、FTPS 双栏文档，规则见[第七节](#七附2026-09-12-目录比较与同步)：

- **目录比较**：双栏文档工具条「比较目录」，在两栏各自选中不同的条目（仅当前一层，与 WinSCP 同名命令同一口径）
- **同步（单向 / 双向 / 镜像 / 仅时间戳）**：扫描 → 预览（可逐项取消勾选）→ 确认删除 → 批量执行 → 自动复查
- **保持远程目录最新**：监视本地改动自动上传

~~原判断：零行代码，需要新增比较结果模型、差异计算、预览 UI、执行引擎。~~
差异计算默认**先比 SHA-256**（服务器端算摘要），不支持或出错时回退到大小 + 修改时间（见 7.4）。

### C2. 搜索能力

`ISftpService` 需要新增递归搜索 API。注意远端递归遍历的成本，
需要流式返回 + 可取消（现有 `ListDirectoryAsync` 是一次性返回 `List`，不适合直接递归）。

### C3. 终端联动

- 文件栏右键无"在此处打开终端"
- 终端当前目录无法同步到 SFTP 栏

SFTP 文档目前是纯双栏（`SftpDocumentView.axaml` 无终端控件）。
本项目**已有完整终端栈**，这一项的架构基础其实是现成的，主要是 UI 编排。

---

## 五、建议的优先级

纯粹从"投入产出比"排，供参考：

**第一梯队（低成本、高感知）**
1. A 类接线债（A1、A2、A3、A5、A6、A7、A8）——用户当成 bug 的那些
2. 键盘快捷键（F5 刷新 / F2 重命名 / Del 删除 / Enter 进入 / Ctrl+A 全选）
3. 可编辑路径输入框
4. 当前目录过滤框

**第二梯队（中等成本、明确需求）**
5. 通配符选择 / 反选
6. 传输队列可视化（A4 + 单条取消重试）
7. 列宽与分栏比例持久化
8. 本地栏补齐（排序、隐藏文件开关、新建文件）
9. 拖放到目标文件夹行

**第三梯队（高成本、差异化）**
10. ~~**目录比较 + 同步**（C1）——决定"是否能替代 WinSCP"~~ —— 2026-09-12 已完成（09-13 补上 SHA-256 优先比较）
11. 远端递归搜索（C2）
12. 终端联动（C3）
13. chown / 时间戳修改（符号链接已于 2026-09-12 完成）
14. 传输后校验

---

## 六、附：2026-07-22 已完成的编辑器增强

内置编辑器（`RemoteFileEditorView`）原先**完全没有语法高亮**。已补：

- **文件类型自动识别**（`Services/Syntax/FileTypeDetector.cs`）：
  扩展名 → 特殊文件名（`Dockerfile`/`sshd_config`/`fstab`/`.bashrc`…）→ **shebang**。
  第三级对远端编辑尤其关键：服务器上大量可执行脚本没有扩展名，只有 `#!/bin/bash` 能说明它是什么。
- **补齐 AvaloniaEdit 缺失的语法定义**（`Syntax/*.xshd`）：
  内置 20 份定义里**恰恰没有** Shell、YAML、INI/conf、Dockerfile、Log ——
  而这五种正是运维日常编辑最多的。已自行编写。
- **主题跟随**（`SyntaxHighlightingService`）：
  AvaloniaEdit 内置定义是给浅色背景调的（关键字 `Blue`、标点 `Black`），
  在本应用的 Dracula 面 `#282A36` 上标点等于隐形。现按命名颜色整体重着色为
  Dracula（暗）/ Alucard（亮），未收录的角色做对比度兜底。

~~**已知限制**：编辑器打开后再切换主题，配色不会实时更新（重新打开即可）。~~ —— 2026-09-12 已解决，见下。

### 2026-09-12 更新：配色跟随具名主题、链接改色、语言扩充、窗口加大

- **配色从当前界面主题派生**：原先只有 Dracula / Alucard 两套，Nord、Gruvbox、GitHub Light 等主题下
  编辑器底色跟着主题走、代码却仍是 Dracula 配色。现在语法色取当前主题的种子色
  （字符串 = Yellow、关键字 = Magenta、数字 = Accent、函数 = Success、类型 = Info、注释 = TextTertiary），
  VelaDark 下与原先的 Dracula 配色一致，VelaLight 下除变量色（改为橙与红的中点，避免与字符串撞色）外与 Alucard 一致；
  每个角色对编辑器底色不足 3:1 时自动向正文色靠拢。
- **网址 / 邮箱链接改色**：AvaloniaEdit 缺省链接色是纯蓝 `#0000FF`，在 Dracula 底上约 1.7:1、几乎读不出来。
  改用信息色令牌 `VelaInfo`（Dracula 青 `#8BE9FD`、Alucard 深青蓝 `#036A96`，其余主题各取自己的 Info），
  全部主题对编辑器底色均 ≥ 3:1（有回归用例逐主题量）。
- **内置定义的颜色名全部归类**：CSS 选择器、HTML 标签、Markdown 链接、Patch 增删行等原先没有角色、
  顶着浅色配色的名字已全部映射；回归用例逐个核对内置与自带定义里的每一个颜色名。
- **新增自带语法**：nginx、TOML、Makefile、Go、Rust、Lua、Ruby、Perl、通用 SQL（MySQL / PostgreSQL / SQLite，
  替代原先只认 SQL Server 方言的 TSQL）、HCL / Terraform；另补一批扩展名映射
  （`.tsx`/`.jsx`、`.scss`/`.less`、`.axaml`/`.resx`、`.jsonl`、systemd 的 `.timer`/`.socket` 等）。
- **按目录认 nginx**：判定改用远端完整路径，`/etc/nginx/conf.d/*.conf`、`sites-available/` 下的文件按 nginx 着色，
  其余目录的 `.conf` 仍按 INI。
- **主题实时切换**：编辑器打开期间切换主题会立即重新着色（订阅 `IThemeService.EffectiveThemeChanged`）。
- **默认窗口加大**：928×648 → 1160×820；小屏或高缩放下打开时按屏幕工作区收缩并居中。
- **已知限制**：AvaloniaEdit 12 内置的 TeX 定义自身损坏（加载即报 "Could not find main RuleSet"），
  `.tex` 暂不映射、按纯文本打开。

---

## 七、附：2026-09-12 目录比较与同步

适用于 SFTP、FTP、FTPS 双栏文档（插件协议的文件文档同样能用，限制见 7.5）。入口在双栏文档顶部的工具条；
终端侧边栏里的文件浏览器没有本地栏，不提供这两个入口。

### 7.1 比较目录（不递归）

- 比较两栏**当前所在的目录**这一层，比较依据取同步窗口上次用的设置（默认修改时间 + 文件大小）。
- 本地栏选中：只有本地有的、本地较新的；远端栏选中：只有远端有的、远端较新的；
  时间相同而大小不同的、以及冲突的，两栏都选中。
- 工具条右侧给出一句结论（「本地 N 项不同 · 远端 N 项不同 · N 项相同」），任一栏换了目录即清除。
- 远端栏隐藏点文件时，本地栏的点文件也不参与比较，免得每个 `.git`、`.env` 都被标成「只有本地有」。

### 7.2 同步窗口

| 选项 | 取值 | 说明 |
| --- | --- | --- |
| 方向 | 本地 → 远端 / 远端 → 本地 / 双向 | 双向时模式固定为「同步文件」，且从不删除 |
| 模式 | 同步文件 / 镜像文件 / 仅同步时间戳 | **同步**：传新增的、较新的、大小不同的，不覆盖较新的目标；**镜像**：只要不同就覆盖，哪怕目标更新；**时间戳**：不传内容，两边都有且大小相同、时间不同的文件，把目标时间改成源的 |
| 比较依据 | SHA-256 校验（默认勾选）、修改时间、文件大小 | 三样都不选 = 只补齐缺失的；SHA-256 的口径见 7.4 |
| 删除多余文件 | 仅单向 | 删除只在目标一侧存在的条目；目录整棵删除，其下条目不再单列；执行前二次确认 |
| 仅已存在的文件 | — | 只更新两边都有的文件，不新建任何东西 |
| 文件掩码 | WinSCP 写法 `包含 \| 排除` | `;` 分隔多个掩码；以 `/` 结尾的是目录掩码；中间含 `/` 的按相对路径匹配；`*` `?` 不区分大小写；`*.*` 等同 `*`。被排除的目录**不扫描**，也就不会被删除 |

流程：**比较**（两边并行扫描，状态栏显示已扫描的目录数，可取消）→ **预览**（每一步一行：操作、路径、两边的大小与时间，
可逐项取消勾选；改动任何选项都会作废预览）→ **同步** → **自动复查**（结论写明「没有剩余差异」或「仍有 N 项待同步」）。

执行规则：

- 顺序：建目录 → 改时间 → 传文件 → 删除。**中途取消后不再执行删除。**
- 传文件与普通上传下载走同一条管道（传输浮窗、全局并发上限、传输日志），但**不走「文件已存在时」的冲突策略，
  也不做续传探测** —— 预览就是确认；目标比源小正是「内容不同」的常态，不能当半截文件去续传。
- 传完**总是**回写修改时间（不看「保留时间戳」设置）：上传后用 SFTP setstat / FTP `MFMT` 把远端时间对齐本地，
  下载后把本地时间对齐远端。不回写的话，下一次比较会把刚传上去的文件判成「远端较新」。服务器不支持时窗口里给出提示。
- 从远端名字拼出的本地路径逐段校验：Windows 上不合法的名字（如 `a:b`、`CON`）报错跳过，而不是写到别处。
- 父目录由执行器逐级补齐：在预览里取消勾选「新建目录」、只留下其中的文件，照样能传。

### 7.3 保持远端目录最新

- 恒为本地 → 远端，沿用窗口里的文件掩码、比较依据与「删除多余文件」（勾了删除会先确认）。
- 开始时先完整对齐一次；之后监视本地目录树，改动防抖 1 秒后**只对变化所在的那一层目录**重新比较，
  新出现的目录（解压、`git clone`）整棵比较；监视缓冲溢出（事件被丢）时整棵重对一遍。
- 列表区换成活动记录（最新在上），记下每次上传与删除；关闭窗口或关闭文档即停止。

### 7.4 比较口径

- **SHA-256 优先**（2026-09-13 补上）：两边都有、**大小相同**的文件先比内容摘要（大小不同已经证明内容不同，不必再读）。
  - 摘要相同 = 同一个文件，**时间不同也不传**（`git checkout`、解压、`touch` 之后不再整片重传）；
    摘要不同 = 内容确实变了，由修改时间判断哪边较新，时间也相同时算「内容不同」（单向照传，双向不动）。
  - 远端摘要在**服务器上算**，不经网络传内容：SFTP 经 SSH exec 通道跑 `sha256sum`（没有则 `shasum -a 256`），
    每批最多 200 个路径；FTP / FTPS 用 `HASH`（SHA-256）或 `XSHA256`。先算远端，服务器不支持时本地一个字节都不读。
  - **回退**：服务器整体不支持或出错（纯 SFTP 账号禁了 exec、主机没有这两个工具、Windows 远端、FTP 服务器不提供这两条命令、
    插件协议）→ 这一轮全部按大小与修改时间比较，窗口附注写明原因；单个文件读不了 → 只有它回退，附注写明「N 个中有 M 个」。
    只勾了 SHA-256 而没勾时间与大小时，回退按两样都比。
  - 时间戳模式下也生效：算出摘要而内容不同的文件，不会被改时间掩盖差异。
  - 摘要按「路径 + 大小 + 修改时间」缓存到窗口 / 文档关闭：同步后的自动复查只重算刚传过的文件（等于顺带做了传输后校验）。
  - 「比较目录」同样先比 SHA-256，回退时结论后面带一句提示。
- **时间精度**：两边先截到较粗的一级再比。SFTP 为秒；FTP 的 Unix LIST 只给到分钟（半年以前的文件只给到日期），
  按原始值推断精度；容差 1 秒（吸收 FAT/exFAT 的 2 秒粒度）。
- **大小写**：Windows / macOS 上路径配对不区分大小写；远端有两个只差大小写的名字时标为冲突、不处理。
- **类型冲突**：一边是文件一边是目录，标为冲突，其下子项不再比较（否则勾上删除会去删一棵没打算动的树）。
- **链接**：两边都不进入指向目录的链接（跳过的个数会提示，与文件夹下载同一口径）；远端指向文件的链接按文件处理；
  本地只跳过真正的符号链接与目录联接，OneDrive「按需下载」的占位文件照常参与。

### 7.5 未做 / 已知限制

- **SHA-256 的代价**：大小相同的文件要在两边各完整读一遍（本地读盘、远端服务器读盘），大目录第一次比较会明显变慢；
  不需要时可取消勾选。缓存不跨窗口：内容改了但大小与修改时间都没变（刻意 `touch -r`）的文件，在同一个窗口里第二次比较时会用到旧摘要。
- 同步选项只在同一个文档标签里记住，不写进设置，重启后回到默认。
- **FTP 时区**：LIST 的时间按服务器当地时间给出、按本机时区解释。服务器与本机不在同一时区时，时间比较会整体偏移 ——
  这时请只勾「文件大小」。WinSCP 在会话设置里提供「时区偏移」，VelaShell 暂无此项。
- 不支持 `MFMT` 的 FTP 服务器与插件协议，上传后无法对齐远端时间：单向「本地 → 远端」不受影响（远端较新不会被当成要上传），
  但**双向同步会把刚上传的文件再下载回来**，窗口会提示这一点。
- 「保持最新」只有本地 → 远端（与 WinSCP 相同）。
