# 凭据管理器集成：设计与实施方案

> 编写日期：2026-09-15（v2 同日修订）　　基准代码：`dev` @ `4ee4c982`　　状态：**核心决策已定，§16 有新增待确认项，待实施**
>
> 用途：让连接配置不再保存密码本身，而是保存一条指向 1Password / Bitwarden / KeePassXC /
> 系统密钥链中某个条目的**引用**，连接那一刻才取密码。
>
> **核实范围**（v2）：
> - 代码侧：连接链路、设置与界面、装配与测试三方面逐文件梳理，关键结论已对照源码抽查。
>   行号基于 `4ee4c982`，会随提交漂移，**以符号名为准**。
> - CLI 侧：已对照三家官方文档与开源源码核对（链接见 §8 各节末尾）。
>   **尚未真机实测**（编写环境未安装 `op` / `bw` / `keepassxc-cli`），标注「待实测」的项必须在阶段 1 开工前跑一遍。
>
> 相关文档：[系统密钥链与 sudo 凭据填充可行性调研](系统密钥链与sudo凭据填充可行性调研.md)（本文的「另一个入口」）、
> [插件生态设想 §2.4 凭据提供方](../plugins/15-ecosystem-ideas.md)、[安全与威胁模型](../plugins/12-security-threat-model.md)。

## 目录

- [0. 结论先说](#0-结论先说)
- [1. 纠正一个前提](#1-纠正一个前提)
- [2. 现状盘点（已核实）](#2-现状盘点已核实)
- [3. 顺带发现的既有缺陷](#3-顺带发现的既有缺陷)
- [4. 已定决策](#4-已定决策)
- [5. 架构](#5-架构)
- [6. 解锁会话缓存](#6-解锁会话缓存)
- [7. CLI 运行器](#7-cli-运行器)
- [8. 三家 provider 规格](#8-三家-provider-规格)
- [9. 系统密钥链 provider](#9-系统密钥链-provider)
- [10. 设置与界面](#10-设置与界面)
- [11. 范围外](#11-范围外)
- [12. 分期与任务清单](#12-分期与任务清单)
- [13. 测试计划](#13-测试计划)
- [14. 文档与仓库同步清单](#14-文档与仓库同步清单)
- [15. 风险登记](#15-风险登记)
- [16. 新增待确认决策](#16-新增待确认决策)
- [17. 实施时再定的小问题](#17-实施时再定的小问题)
- [18. 修订记录](#18-修订记录)

---

## 0. 结论先说

**已定决策（2026-09-15）**

| 项 | 定稿 |
|---|---|
| 抽象 | 新增 `ICredentialProvider`（凭据**来源**），**不**改造 `ISecretProtector`（凭据**加密**） |
| 首批 provider | 1Password（`op`）、Bitwarden（`bw`）、KeePassXC（`keepassxc-cli`）**三家一起交付** |
| 私钥 | **走 SSH Agent**，不经 CLI 取私钥。等上游 PR 合并后另行实施 |
| 引用粒度 | **整条配置绑定一个条目**；高级选项允许用本配置的用户名覆盖条目用户名 |
| 解锁口令 | **内存 TTL 缓存**，时长可配置 |
| 解析失败 | **退回登录弹窗 + 顶部提示条** |
| 引用与已存密码 | **禁止并存** |
| 形态 | **宿主内置**，不开放插件 SDK 契约 |
| 与 `IMasterKeyStore` | **并行推进**；系统密钥链 provider 等它的 P/Invoke 层 |

**v2 新增的关键结论**

1. **明文密码有三种载体、五个构建点**，不止 `BuildChainAsync` 一处（§2.1）。FTP 和三类插件协议都不经过 `ConnectionWorkflowService`，解析器必须接到每一个构建点上。
2. **发现四处既有缺陷**（§3），其中一处会让「记住密码」没勾时，用户手输的密码照样被写进数据库。建议作为**阶段 0a** 单独修掉，否则本功能的「引用配置不落盘密码」会建在一个漏水的地基上。
3. **自动重连不能弹解锁框**。网络恢复后 N 个标签同时重连，会同时触发解锁。解析要区分交互式与非交互式（§5.7）。
4. **「引用配置不落盘密码」这条不变量放在仓储层强制**。仓储是所有保存的唯一出口，比在各个调用点逐一防守可靠（§5.4）。
5. **1Password 在 Windows 上按进程链授权，而且这一点没有明确文档**。VelaShell 拉起 `op` 会不会每次都弹 Windows Hello，是阶段 1 的**第一个实测项**，结果直接决定调用次数的设计（§8.1）。
6. **Bitwarden 的 `bw list items` 会连同密码一起输出**；**KeePassXC CLI 只能按路径寻址，标题重名时取第一个匹配项**（§8.2 / §8.3）。

---

## 1. 纠正一个前提

`feature-plan.md` 原先的表述是：「`ISecretProtector` 就是那层抽象，加一个 `ICredentialProvider` 让它可插拔即可」。这句话只对了一半。

- `ISecretProtector` 管的是**怎么加密**：一个同步的 `string → string` 变换。
- 凭据管理器集成管的是**秘密放在哪、归谁管**：配置里存引用，而不是值。

两者是不同维度，不能共用一个接口：

| 若让 `ISecretProtector` 去调 `op` / `bw` | 后果 |
|---|---|
| `GetAllSessionsAsync()` 每次加载都对**所有**配置调 `Unprotect`（`SonnetDbSessionRepository.cs:38`） | 启动、刷新会话树时起 N 个 CLI 进程，可能连弹 N 次生物识别 |
| 接口是同步的，调用方有 5 处（会话、Gist PAT、两处插件机密、插件信任） | 要么全仓传染 async，要么在 UI 线程上阻塞等外部进程 |
| `PluginTrustRepository` 靠 AES-GCM 认证标签做完整性边界（`PluginTrustRepository.cs:37`） | 换成外部保险库后，这道边界不复存在 |
| `Unprotect` 解不开时**原样返回输入**（`AesSecretProtector.cs:79`） | 解析失败时，`op://vault/item` 会被当作密码发给远端服务器 |

正确的结构是两道接缝，各管一层：

```
                ┌──────────── 接缝 B：凭据来源（本文） ───────────────┐
 SessionProfile │ 存「引用」CredentialReference {provider, item}      │
                │        │ 连接那一刻才解析                           │
                │        ▼                                            │
                │ ICredentialProvider ─ 1Password / Bitwarden /       │
                │                       KeePassXC / 系统密钥链        │
                └─────────────────────────────────────────────────────┘
                ┌──────────── 接缝 A：主密钥存放（密钥链调研阶段 0/1）─┐
 已存的密码     │ ISecretProtector（不改） ← IMasterKeyStore           │
                │                            ├ DPAPI / Keychain /     │
                │                            └ libsecret / 文件回退   │
                └─────────────────────────────────────────────────────┘
```

「与系统密钥链调研是同一件事的两个入口」在这里的具体含义：

- 调研阶段 0/1 的 **`IMasterKeyStore`** 保护的是 VelaShell **自己存下的**密文，与本文互不依赖，并行推进。
- **系统密钥链作为 `ICredentialProvider`**，是让用户引用自己在系统凭据管理器里**已有**的条目。它与 `IMasterKeyStore` 共用同一层 P/Invoke，所以排在后面。
- 调研**阶段二（sudo 填充）**的凭据选择器，直接复用 provider 的 `SearchAsync` / `ResolveAsync`。

---

## 2. 现状盘点（已核实）

### 2.1 明文密码的三种载体

| 载体 | 构建点 | 走 `ConnectionWorkflowService`？ | 送达 |
|---|---|---|---|
| `ConnectionInfo`（`Core/Models/ConnectionInfo.cs`） | `ConnectionWorkflowService.BuildChainAsync`（`:213`，直连 `:224`、链式 `:249`）。跳板每跳从仓储重新读取（`:241`） | 是 | `InfrastructureServiceCollectionExtensions.AddCredential`（`:327` → `new PasswordCredential(ci.Password ?? "")`），跳板经 `BuildProxyChain`（`:445`） |
| `FtpConnectionInfo`（`Core/Ftp/FtpConnectionInfo.cs`） | `FtpConnectionInfo.FromProfile`（`:50`），唯一生产调用点 `MainWindowViewModel.cs:3684` | **否** | `FtpFileService.CreateConnectedClientAsync`（`:627`，代理路线 `:639`） |
| SDK 请求 DTO | `PluginProtocolFileService.OpenSessionCoreAsync`（`:100-108`，`ProtocolConnectRequest`）<br>`PluginTerminalShellStream`（`:218-226`，`ProtocolConnectRequest`）<br>`PluginWorkspaceLauncher.OpenCoreAsync`（`:127-140`，`WorkspaceConnectRequest`） | **否** | **进程内插件**的代码。隔离模式的 PluginHost 不允许注册协议或工作台（`RemotePluginContext.cs:92-102,155-181`） |

`ConnectionInfo` 只在 `BuildChainAsync` 里构建（`src` 范围内）。所以 SSH 一侧只有这一个点，FTP 与插件协议另有四个点，**总共五个构建点**。

### 2.2 连接入口全表

「解析点」一列说明本方案在哪里把引用变成密码（见 §5.5）。

| 入口 | 位置 | 用的 profile 实例 | 解析点 |
|---|---|---|---|
| SSH 终端 | `TryConnectProfileAsync` `MainWindowViewModel.cs:3194-3342` → `RunHandshakeAsync` `:2304` → `ConnectProfileAsync` `:2316` | 调用方传入 | `BuildChainAsync` |
| 原地重连（Ctrl+R / 回车 / 提示气泡按钮 / 自动重连 / 唤醒恢复） | `ReconnectTabAsync` `:2394-2518` → `ConnectProfileAsync(tab.Profile)` `:2455`；调用方：命令 `:872`、`ReconnectRequested` `:1246/2259`、气泡 `:2782`、`OnTabDisconnected` 定时器 `:2812-2832`、`ReconnectAllAfterResume` `:2694-2713` | `tab.Profile`（内存） | `BuildChainAsync`，**自动重连必须用非交互模式**（§5.7） |
| 克隆标签 | `:879-895` → `TryConnectProfileAsync(ActiveTerminalTab.Profile)` `:888`（同一实例，未 `Clone`） | `tab.Profile` | 同上 |
| 独立 SFTP 文档 | `OpenSftpDocumentForProfileAsync` `:3476-3571` → `ConnectProfileAsync` `:3519` | 循环里的 `current` | `BuildChainAsync` |
| 隧道面板后台连接 | `ConnectTunnelHostAsync` `:1767-1791`；隧道自动重连 `TunnelPanelViewModel.AutoReconnectAsync` `:591-600` | 隧道面板服务器列表 | `BuildChainAsync`，自动重连用非交互模式 |
| 工作台插件的 SSH 跳板隧道 | `EstablishWorkspaceTunnelAsync` `:4276-4339`（跳板配置从仓储读取 `:4304`，可能复用已连会话 `:4310`） | 仓储实例 | `BuildChainAsync` |
| 最近连接 / 命令面板 / 会话树 / 启动恢复 / 配置弹窗「连接」 | `TryConnectRecentAsync` `:4531`、面板 `:2192`、`SessionTreeViewModel` → `MainWindow.axaml.cs:446`、恢复 `MainWindow.axaml.cs:526-578`、弹窗 `MainWindow.axaml.cs:1187` | 各自缓存 / 仓储实例 | 按协议分流 |
| 外部拉起（URL / 命令行 / `.xsh`） | `ExternalLaunchCoordinator.cs:66` → `ResolveProfileAsync` `:223-268`（请求不带密码时复用已存配置 `:225`） | 已存或临时配置 | 按协议分流 |
| 插件 `Sessions.OpenAsync`（AI 插件 `open_session`） | `SessionsCapability.OpenAsync` `:91-145` → `HostSessionOpener.cs:48` → `TryConnectProfileAsync` | 仓储实例；插件只拿到 `SessionInfo`，**不含凭据** | `BuildChainAsync` |
| 测试连接 | `ConnectionProfileViewModel.cs:1202` → `TestConnectionAsync` `ConnectionWorkflowService.cs:57-88` | `BuildProfile()` 新对象 | `BuildChainAsync`；插件协议走 `ProbePluginConnectionAsync` `:4201-4245` |
| 连接诊断第 4 步 | `ConnectionDiagnosticsService.cs:236` → `TestConnectionAsync`；`HasStoredCredentials` `:351-360` 决定是否跳过 | 树缓存或 `tab.Profile` | `BuildChainAsync` |
| FTP / FTPS | `OpenFtpDocumentForProfileAsync` `:3641-3739` → `FromProfile` `:3684` | 循环里的 `current` | **`MainWindowViewModel` 构建 `FtpConnectionInfo` 之前** |
| 插件文件系统协议（S3 等） | `OpenPluginDocumentForProfileAsync` `:3771-3903` → `_pluginProtocols.OpenSessionAsync` `:3834` | 循环里的 `current` | `PluginProtocolFileService` |
| 插件终端协议（Telnet / 串口）及其重连 | `OpenPluginTerminalForProfileAsync` `:3923-3960`；重连 `ReconnectPluginTerminalAsync` `:3997-4033` | `tab.Profile` | `PluginTerminalShellStream` |
| 插件工作台（Redis 等） | `OpenWorkspaceDocumentForProfileAsync` `:4055-4185` → `launcher.OpenAsync` `:4124` | 循环里的 `current` | `PluginWorkspaceLauncher` |

**复用已有连接、不重新认证的面板**（不受影响）：SFTP 侧栏与远程编辑（在已有 client 上开通道，`InfrastructureServiceCollectionExtensions.cs:181-195`）、资源监视、进程管理、隧道、路由追踪。

`SessionProfile.AutoStartTunnelIds` 目前**没有任何读取方**，不存在「隧道自动启动」这条连接路径。

### 2.3 「缺凭据」判定点

| 判定 | 位置 | 调用方 | 本方案改动 |
|---|---|---|---|
| `RequiresCredentials` | `MainWindowViewModel.cs:3174-3187` | `:1777` 隧道、`:3245` SSH 终端、`:3496` SFTP | 有引用视为不缺密码；`OverrideUsername == false` 时也视为不缺用户名 |
| `RequiresPluginCredentials` | `:4433-4436` | `:3812`、`:4095` | 同上 |
| `RequiresFtpCredentials` | `:4492-4494` | `:3661` | 同上 |
| `ConnectionWorkflowService.ValidateProfile` | `ConnectionWorkflowService.cs:258-307`，密码为空时**抛异常**（`:286-289`） | `ConnectProfileAsync` `:94`（要求凭据）、`SaveProfileAsync` `:40`（不要求） | 有引用时跳过密码非空校验 |
| `ConnectionDiagnosticsService.HasStoredCredentials` | `:351-360` | 第 4 步 `:223-231` | 有引用视为有凭据 |
| `ExternalLaunchCoordinator` | `:225` | — | 无需改动：请求不带密码时复用已存配置，已存配置带引用即可 |

**没有判定、也不弹框的路径**：`ReconnectTabAsync`、插件终端打开与重连、`ProbePluginConnectionAsync`、`EstablishWorkspaceTunnelAsync` 的跳板连接、`BuildChainAsync` 的跳板各跳。

### 2.4 重试循环与登录弹窗

- **弹窗委托**：`MainWindowViewModel.InteractiveAuthenticator`（`:589`）在 `MainWindow.axaml.cs:407` 设为 `PromptCredentialsAsync`。后者经 `_connectPromptGate` 信号量串行化（`:1479-1491`），核心是 `PromptCredentialsCoreAsync`（`:1496-1546`）。
- **⚠️ 弹窗就地修改传入的实例，并返回同一个实例**（`:1526-1545`）：`Username`、`AuthMethod`、`Password`（`:1533`）、`RememberPassword`（`:1535`）、`PrivateKeyPath`、`PrivateKeyPassphrase`、`CertificatePath`。传入的实例可能是会话树缓存、命令面板缓存、`tab.Profile`、隧道面板列表项。
- **五个重试循环**，都是局部的 `for (int attempt = 0; attempt < 3; attempt++)`：

| 循环 | 位置 | 认证失败的处理 | 证书信任 |
|---|---|---|---|
| SSH 终端 | `:3243-3341` | `VelaSshAuthenticationException` 且有弹窗委托 → `continue`（`:3324-3328`） | — |
| SFTP | `:3494-3570` | 静默断开、记录后 `continue`（`:3553-3558`） | — |
| FTP | `:3659-3738` | `VelaFtpAuthenticationException` → `continue`（`:3723`） | 信任后 `PersistProfileIfSavedAsync` + `attempt--`（`:3712-3714`） |
| 插件文件系统 | `:3810-3902` | `PluginProtocolAuthenticationException` → `continue`（`:3887`） | 同上，最多追加 2 次（`:3872-3878`） |
| 插件工作台 | `:4093-4184` | 同上（`:4169`） | 同上（`:4152-4161`） |

- 判定逻辑都是 `needsPrompt = attempt > 0 || Requires*(current)`，所以第一次失败之后必然弹框。
- 四个文档路径失败卡片上的「重试」闭包，捕获的都是**最初传入的 `profile`**（`:3492,3657,3787,4069`）。今天之所以不用重新输入，靠的是弹窗就地修改了那个实例。

### 2.5 持久化点全表

仓储只有一个实现 `SonnetDbSessionRepository`：`SaveSessionAsync` → `Protect`（`:131-140`）先克隆再加密；读取时 `Unprotect`（`:167-173`）**原地解密**，所以任何仓储读取拿到的都是明文。

| 保存点 | 位置 | 是否遵守「记住密码」 |
|---|---|---|
| `ConnectionWorkflowService.SaveProfileAsync` | `:41` | 是（经 `WithPersistablePasswordAsync`） |
| `ConnectionWorkflowService.ConnectProfileAsync` 成功后 | `:109` | 是 |
| `MainWindowViewModel.PersistProfileIfSavedAsync` | `:4519`（调用方：FTP `:3713`、插件 `:3871`、工作台 `:4156`） | **否**，直接保存 |
| `SessionTreeViewModel.MoveSessionToGroupAsync`（拖动分组） | `src/VelaShell.Presentation/ViewModels/SessionTreeViewModel.cs:443` | **否**，保存的是树缓存实例 |
| `SessionTreeViewModel.DuplicateSelectedSessionAsync`（复制配置） | 同文件 `:625` | **否**，`Clone()` 会复制 `Password` |
| Gist 同步拉取应用 | `GistSyncService.cs:617-626` | **否**，传入无密码时拷回本机已存密码后直接保存 |
| 会话导入 | `SessionImportWriter.cs:52-61`、`:126` | 按导入结果设置 `RememberPassword`，直接保存 |
| 配置弹窗保存 | `ConnectionProfileViewModel.SaveAsync` `:1167` → workflow | 是 |
| AI / 插件连接提议 | `MainWindow.axaml.cs:1106-1137` → 配置弹窗 | 是（经弹窗） |

`WithPersistablePasswordAsync`（`:121-146`）**只剥离 `Password`，不剥离 `PrivateKeyPassphrase`**（`:144`）。

### 2.6 明文在内存中的驻留

| 位置 | 驻留时长 | 读取方 |
|---|---|---|
| `TerminalTabViewModel.Profile`（`:125`） | 标签生命周期 | 重连、克隆、诊断 |
| `SshSession.ConnectionInfo`（`SshSession.cs:18`，`SshConnectionService.cs:185-188` 赋值） | 会话生命周期，**含每一跳跳板的密码** | 只读 Host / Port / Username（`SessionsCapability.cs:36-38`、`SessionMetricsService.cs:139`、`RemoteProcessService.cs:125`、`MainWindowViewModel.cs:4312`） |
| `TmdsSshClientWrapper._settings`（`:37`）中的 `PasswordCredential` | 会话生命周期 | 不会自行重连（`AutoConnect = false`，DI `:299`） |
| `FtpConnectionPool.Info`（`:43`）+ 工厂闭包（`FtpFileService.cs:44`） | 连接池生命周期，**按需新开连接时反复使用** | 连接池 |
| 会话树 `_sessionCache`、命令面板 `_paletteProfiles`、隧道面板 `Servers` | 直到下次刷新 | 被弹窗就地修改后会带上手输密码 |
| `SftpDocumentViewModel.Profile`、`ConnectingDocument.Profile`、`PluginWorkspaceDocument.Profile` | 文档生命周期 | — |

### 2.7 其它约束

| 约束 | 依据 | 对本方案的影响 |
|---|---|---|
| `AuthMethod` 落盘的是序号，只能末尾追加 | `AuthMethod.cs`、`ModelSerializationTests.AuthMethod_OrdinalValues_MustStayStable` | 「来源」做独立字段，**不加** `AuthMethod.PasswordManager` |
| 新增 `SessionProfile` 字段要同时补 `Clone()` 与测试的 `FullyPopulated()` | `tests/VelaShell.Core.Tests/Models/SessionProfileCloneTests.cs:25-61` | 否则默认值恰好相等时测试不会报红 |
| Gist 同步未加密时清掉 `Password` / `PrivateKeyPassphrase` | `GistSyncService.cs:490-494` | 引用不是机密，可以随同步漫游 |
| 本机字段在推送时剔除、拉取时保留，是一对**镜像方法** | `ScrubDeviceLocalFields` `:681-696` / `PreserveDeviceLocalFields` `:699-711` | 本机路径若放进 `AppSettings`，两边都要加（§10.1 建议干脆不放） |
| 配置导出只清空代理密码 | `SettingsViewModel.BuildExportJson` `:1334-1339` | 同上 |
| **ConPTY 本地终端继承 VelaShell 整个进程环境** | `ConPtyShellStream.cs:223-226`（`lpEnvironment = IntPtr.Zero`） | **绝不能设置进程级环境变量**，否则 `BW_SESSION` 会漏进用户的 shell |
| 仓库刻意不引用 `Microsoft.Win32.SystemEvents` | `ConnectivityMonitor.cs:29-33` | 「锁屏 / 休眠时清缓存」要么复用 `ConnectivityMonitor.Resumed`，要么推翻这个取舍（§16 N6） |
| 没有通用的「起进程 + 超时 + 抓输出」封装 | 各调用点自行处理；参考 `PluginProcessClient.cs:233-374` | `CliCredentialRunner` 要新写 |
| 没有任何可执行文件签名校验 | 更新包也只校验 SHA-256 | Authenticode 校验不能照抄现成代码；1Password App 自己会校验 `op.exe`（§8.1） |
| `SecureStringConvert` 只在 App 工程里 | `src/VelaShell/Security/SecureStringConvert.cs` | Infrastructure 拿不到，§16 N7 建议下沉到 Core |
| 全仓没有 `CryptographicOperations.ZeroMemory` | — | 解锁缓存清零要新写 |
| 应用未开启裁剪、未做 AOT 发布 | `VelaShell.csproj:40` | 解析 CLI 的 JSON 可以用反射式 System.Text.Json / `JsonDocument` |
| 没有分层架构测试，分层只靠项目引用 | — | 新契约放 Core，实现放 Infrastructure，界面放 App |
| 所有服务都是单例；多实现注册靠「先注册具体类型，再转发」 | `InfrastructureServiceCollectionExtensions.cs:70-81` | provider 注册照此写 |
| 界面实现的接口经 `sp.GetService<T>()` 可选解析，缺失时 fail-closed | `IHostKeyPrompt`：`App.axaml.cs:136`、DI `:125` | 解锁弹窗照此接入 |
| 集成测试跳过用 `Assert.Inconclusive`，不再是 `[SKIP]` 行 | `SshIntegrationTests.cs:90-117`、`CrossPlatformPublishTests.cs:92-105` | 真实 CLI 测试照此门控 |
| 插件路线的既有结论 | [15 §2.4](../plugins/15-ecosystem-ideas.md)、[12 §1](../plugins/12-security-threat-model.md) | 内置方案不经 PluginHost / RPC，威胁模型的承诺保持成立 |
| Tmds.Ssh 0.24 支持认证时回调取密码、支持内存私钥 | 包内 XML：`PasswordCredential(PasswordPrompt)`、`PrivateKeyCredential(Func<CancellationToken, ValueTask<Key>>)` | 本期不用（私钥走 Agent、密码提前解析），记录备查 |

---

## 3. 顺带发现的既有缺陷

这四处与凭据管理器无关，今天就存在。但本功能会放大其中几处，所以**建议作为阶段 0a 单独修掉**（§16 N1）。

### D1 · 「记住密码」没勾，手输的密码仍可能落盘

**复现路径**（按代码推演，**待实测确认**）：

1. 某配置 `RememberPassword = false`，库里没有密码。
2. 用户在会话树上双击连接。`RequiresCredentials` 为真，弹出登录弹窗，用户输入密码且保持不勾「记住」。
3. 弹窗把密码写进**会话树缓存里的那个实例**（`MainWindow.axaml.cs:1533`）。连接成功后，workflow 按「记住密码」规则剥离密码再保存，这一步没有问题。
4. 用户随后把这条配置**拖进另一个分组**。`MoveSessionToGroupAsync` 直接保存缓存实例（`SessionTreeViewModel.cs:443`），**密码被加密写进数据库**。
5. 同理，「复制配置」（`:625`）、FTP / 插件证书信任后的 `PersistProfileIfSavedAsync`（`:4519`）也会把手输密码落盘。

**影响**：违背用户的明确选择。凭据被静默持久化，而且界面上看不出来。

**修法**（与 D3 一起修）：

- 登录弹窗改为**返回副本**，不再修改传入实例。
- 失败卡片的「重试」闭包改为捕获最新的 `current`，保留今天「重试不用重输」的体验。
- `PersistProfileIfSavedAsync` 改走 workflow 的剥离逻辑。
- 弹窗不再修改共享实例之后，会话树缓存里只剩仓储读出来的值（本来就已落盘），`MoveSessionToGroupAsync` 与复制配置自然不会再多存东西，**不必逐个改**。

### D2 · `WithPersistablePasswordAsync` 不剥离私钥口令

- `:144` 只写了 `stripped.Password = null`。
- 配置弹窗的「记住密码」勾选框只在密码认证时显示（`ConnectionProfileView.axaml:531-546`，受 `ShowPasswordField` 控制）；登录弹窗的私钥分支也不回写 `RememberPassword`。所以私钥口令**事实上总是会落盘**。
- 这可能是有意为之（口令在配置页填写，默认就该保存），也可能是遗漏。**需要你确认语义**（§16 N2），再决定是补上剥离，还是在注释里写明「口令不受记住密码控制」。

### D3 · 登录弹窗就地修改共享实例

见 §2.4。除了造成 D1，还会让明文密码散落在会话树缓存、命令面板缓存、隧道面板列表里，直到下次刷新。修法与 D1 相同。

### D4 · `SshSession.ConnectionInfo` 长期持有全部跳板密码

- 会话存活期间，`SshSession.ConnectionInfo` 一直保存完整的 `ConnectionInfo`，包括每一跳的密码与私钥口令，而读取方只用 Host / Port / Username（§2.6）。
- **修法**：`SshConnectionService.ConnectInternalAsync` 把 `ConnectionInfo` 交给 client 工厂之后，给 `SshSession` 挂一份**去掉机密的副本**（递归处理 `JumpHost`）。
- 这是低风险的加固。与本功能的关系：解析出来的保险库密码也会进入 `ConnectionInfo`，不加固的话它会驻留整个会话生命周期。

---

## 4. 已定决策

| # | 问题 | 定稿 | 理由 / 影响 |
|---|---|---|---|
| 1 | 首批支持哪几家 | **三家一起** | CLI 运行器与解锁会话组件在阶段 1 一次做完 |
| 2 | 私钥怎么取 | **SSH Agent** | 私钥不出保险库进程。**等上游 PR 合并**后单独立项；本期 provider 只供给用户名与密码 |
| 3 | 引用粒度 | **整条绑定**，高级选项可覆盖用户名 | 条目改名、改密码都不影响配置 |
| 4 | 解锁口令缓存 | **内存 TTL，默认值可配置** | 见 §6 |
| 5 | 解析失败时 | **退回弹窗 + 提示条** | 见 §5.8 |
| 6 | 引用与已存密码并存 | **禁止** | 在仓储层强制（§5.4） |
| 7 | 内置还是插件 | **内置** | 威胁模型不变，不需要发 SDK |
| 8 | 与 `IMasterKeyStore` 的先后 | **并行** | 系统密钥链 provider 等它的 P/Invoke 层 |

---

## 5. 架构

### 5.1 分层与落点

| 层 | 新增内容 | 目录 |
|---|---|---|
| Core | 契约、模型、异常、解析选项 | `src/VelaShell.Core/Credentials/`（新目录） |
| Core | `SecureStringConvert` 下沉（§16 N7） | `src/VelaShell.Core/Security/` |
| Infrastructure | `CliCredentialRunner`、三个 provider、`CredentialResolver`、解锁会话缓存、provider 设置存储 | `src/VelaShell.Infrastructure/Credentials/` |
| Infrastructure | DI 注册 | `InfrastructureServiceCollectionExtensions.cs`，新增一段 `AddCredentialProviders` |
| Presentation | `ConnectionWorkflowService` 接入解析器 | 现有文件 |
| App | 解锁弹窗、条目选择器、设置页、配置页与登录弹窗改动 | `src/VelaShell/Views/`、`ViewModels/`、`Services/` |

依赖方向照旧：Core ← Infrastructure ← App，Core ← Presentation ← App。Presentation **不引用** Infrastructure，只依赖 Core 里的 `ICredentialResolver`。

### 5.2 契约（`VelaShell.Core/Credentials/`）

```csharp
public interface ICredentialProvider
{
    /// "1password" / "bitwarden" / "keepassxc" / "os-keychain"。落盘在引用里，定下后不可改。
    string Id { get; }

    /// 本地化后的显示名。
    string DisplayName { get; }

    /// 探测安装与登录状态。**不得触发生物识别或解锁弹窗**（设置页一打开就会调用，见 §8.1）。
    ValueTask<CredentialProviderStatus> GetStatusAsync(CancellationToken ct);

    /// 只返回元数据，绝不返回机密。可能需要解锁（交互式）。
    Task<IReadOnlyList<CredentialItemSummary>> SearchAsync(string query, CredentialResolveOptions options, CancellationToken ct);

    /// 取用户名与密码。
    Task<ResolvedCredential> ResolveAsync(CredentialReference reference, CredentialResolveOptions options, CancellationToken ct);

    /// 丢弃本 provider 的解锁缓存（设置页「立即锁定」、TTL 到期、网络恢复时调用）。不调用各家 CLI 的 lock 命令（见 §8.2）。
    void ForgetUnlock();
}

public enum CredentialProviderState { NotInstalled, NotSignedIn, Locked, Ready, Unknown }

public sealed record CredentialProviderStatus(
    CredentialProviderState State,
    string? Version,
    string? ExecutablePath,
    string? Detail);                    // 已本地化的补充说明，给设置页显示

public sealed record CredentialItemSummary(
    string ItemId,
    string? Scope,                      // 1Password: "<accountId>/<vaultId>"；KeePassXC: 数据库别名；其余 null
    string Title,
    string? Username,                   // 列表里拿不到时为 null，选中后再补（§8.1）
    string? Location,                   // 1Password vault 名 / KeePassXC 分组路径 / Bitwarden 文件夹
    string? Url);

public sealed record ResolvedCredential(string? Username, string Password);

public sealed record CredentialResolveOptions(
    bool Interactive,                   // false：需要解锁时直接抛 Locked，绝不弹框（§5.7）
    string TargetDisplayName);          // 解锁弹窗里显示「为了连接 xxx」；遵守「不带用户名与端口」的显示约定

public enum CredentialFailure
{
    NotInstalled, NotSignedIn, Locked, UnlockCancelled, UnlockFailed,
    NotFound, Ambiguous, MissingField, Timeout, Malformed, ProviderDisabled
}

public sealed class CredentialProviderException(
    CredentialFailure kind, string providerId, string? detail = null, Exception? inner = null)
    : Exception(detail, inner)
{
    public CredentialFailure Kind { get; } = kind;
    public string ProviderId { get; } = providerId;
}

/// UI 层实现（与 IHostKeyPrompt 同一套路），fail-closed：异常或无主窗口时返回 null。
public interface ICredentialUnlockPrompt
{
    /// 返回的 char[] 由调用方负责清零。null = 用户取消。
    Task<char[]?> RequestUnlockSecretAsync(CredentialUnlockRequest request, CancellationToken ct);
}

public sealed record CredentialUnlockRequest(
    string ProviderId,
    string ProviderDisplayName,
    string? TargetName,                 // KeePassXC 数据库别名；Bitwarden 为账户邮箱（bw status 可得）
    string ConnectionDisplayName,
    bool PreviousAttemptFailed);        // 口令错时再弹一次，弹窗显示错误提示

/// 工作流与 Infrastructure 的唯一入口。
public interface ICredentialResolver
{
    /// profile 没有引用时返回 null。结果不回写 profile。
    Task<ResolvedCredential?> ResolveAsync(SessionProfile profile, CredentialResolveOptions options, CancellationToken ct);

    IReadOnlyList<ICredentialProvider> Providers { get; }
}
```

**关于回调形式**：`ICredentialUnlockPrompt` 返回 `char[]` 而不是 `SecureString`。因为口令最终要写进子进程的 stdin 或环境变量，`SecureString` 在这里只会多一次转换，不增加实际保护。弹窗内部仍然用 `SecurePasswordBox`（它承载的是 `SecureString`），交出前转成 `char[]`。

### 5.3 数据模型

`SessionProfile` 新增：

```csharp
/// 凭据来源：非 null 时密码从密码管理器取，Password 永不落盘（仓储层强制）。
public CredentialReference? CredentialSource { get; set; }
```

```csharp
public sealed record CredentialReference
{
    public required string ProviderId { get; init; }
    public required string ItemId { get; init; }
    public string? Scope { get; init; }
    public string? DisplayName { get; init; }        // 选择时的条目标题快照，仅显示
    public string? DisplayUsername { get; init; }    // 选择时的条目用户名快照，仅显示
    public bool OverrideUsername { get; init; }      // true：用 SessionProfile.Username 覆盖条目用户名
}
```

各 provider 的 `ItemId` / `Scope` 取值：

| provider | `ItemId` | `Scope` | 说明 |
|---|---|---|---|
| 1Password | item UUID | `<accountId>/<vaultId>` | 官方建议按 ID 寻址，避免重名与限流（§8.1） |
| Bitwarden | item GUID | null | `bw` 同一时间只登录一个账户 |
| KeePassXC | 条目完整路径（`/分组/标题`） | 数据库别名（设置里配置） | CLI **不支持 UUID 寻址**（§8.3） |
| 系统密钥链 | Windows 为 TargetName | null | 阶段 2 |

实施要点：

- `CredentialReference` 是 record，`Clone()` 里直接引用赋值即可（不可变），同步补 `SessionProfileCloneTests.FullyPopulated()`。
- JSON 落盘经 `SonnetDbJson`（camelCase、写入时省略 null），老数据没有这个字段即为 null，**零迁移**。
- 引用**明文落盘、随 Gist 同步**。条目标题、用户名快照、vault ID 属于元数据，不是机密，在 `PRIVACY.md` 如实说明（§14）。

### 5.4 持久化不变量的唯一闸口：仓储

在 `SonnetDbSessionRepository.Protect`（`:131-140`）里加一行：

```csharp
if (copy.CredentialSource is not null)
{
    // 决策 6：引用与已存密码不并存。放在仓储而不是调用方 —— 保存点有 7 处（§2.5），
    // 其中 4 处绕过 workflow，逐个防守迟早漏一个。
    copy.Password = null;
}
```

理由：仓储是所有保存的**唯一出口**。§2.5 列出的直接保存、同步拉取时拷回本机密码（`GistSyncService.cs:623`）、导入、复制配置，都自动被这一行覆盖。`WithPersistablePasswordAsync` 里不需要再加同样的判断。

### 5.5 解析点：五个构建点逐一接线

| # | 构建点 | 改法 |
|---|---|---|
| 1 | `ConnectionWorkflowService.BuildChainAsync` | 构造函数新增可选的 `ICredentialResolver? credentialResolver = null`。每一跳：`profile.CredentialSource != null` 且内存中 `Password` 为空时，调用 resolver 填进 `ConnectionInfo.Username` / `Password`。**不回写 profile**。`ConnectProfileAsync` 与 `TestConnectionAsync` 增加 `CredentialResolveOptions` 参数（默认交互式） |
| 2 | FTP：`MainWindowViewModel.cs:3684` | 构建 `FtpConnectionInfo` 之前先解析。`FromProfile` 新增重载 `FromProfile(SessionProfile, ResolvedCredential?)` |
| 3 | `PluginProtocolFileService.OpenSessionCoreAsync` | 构造时注入 resolver（Infrastructure 内部，DI 可得）。组装 `ProtocolConnectRequest` 前解析 |
| 4 | `PluginTerminalShellStream` | 同上。它的重连也经过这里 |
| 5 | `PluginWorkspaceLauncher.OpenCoreAsync` | 同上 |

**优先级规则**：**内存中非空的 `Password` 优先于引用**。

- 弹窗手输的密码存在于本次连接的副本里（D1/D3 修好之后），自然生效。
- 仓储读出来的实例因为 §5.4 的不变量，永远不会同时有引用和密码。
- 所以这条规则只在「本次手输」时起作用，不会产生歧义。

**跳板各跳**：跳板配置从仓储读取，带引用就用**当前这次连接的交互模式**去解析。

- 解析失败时**不弹登录弹窗**（今天跳板也从来不弹），直接以连接错误结束。
- 错误信息写明是哪一跳的哪个 provider 出了问题，例如「跳板 bastion-01：Bitwarden 保险库已锁定」。

### 5.6 解析器内部行为（`CredentialResolver`）

1. **找 provider**：按 `ProviderId` 查找；未注册或在设置里被禁用时抛 `ProviderDisabled`。
2. **单次尝试缓存**：以「连接尝试令牌 + 引用」为键缓存 `ResolvedCredential`，尝试结束（成功或放弃）即丢弃。
   - 这样同一次尝试里，SSH 跳板链、重试循环中的非认证错误都不会重复查保险库。
   - 实现上，令牌由 `TryConnect*` 循环创建，经 `CredentialResolveOptions` 传下去；也可以用 `AsyncLocal` 承载，实施时取其简单者。
3. **按 provider 串行化解锁**：每个 provider 一把 `SemaphoreSlim(1,1)`。
   - 启动恢复 10 个标签、跳板多跳命中同一个锁定的保险库时，只弹一次解锁框，其余等待后复用同一个解锁会话。
   - 用户取消后，排队的其余请求**立即以 `UnlockCancelled` 结束**，不再逐个弹框。
4. **不缓存解析出的密码**：跨尝试（例如一小时后手动重连）会重新解析。保险库的解锁会话还在 TTL 内，就不会弹框。
5. **用户名规则**：`OverrideUsername` 为 true 时用 `SessionProfile.Username`，否则用条目用户名。两者都为空时抛 `MissingField`。

### 5.7 交互式与非交互式解析

| 触发来源 | 模式 | 保险库锁定时 |
|---|---|---|
| 用户主动连接：会话树、命令面板、最近连接、配置弹窗「连接」/「测试」、克隆标签、打开 SFTP / FTP / 插件文档、外部拉起、启动时恢复会话 | **交互式** | 弹解锁框 |
| 手动重连：Ctrl+R、回车、提示气泡上的「重连」按钮（`:872`、`:1246/2259`、`:2782`） | **交互式** | 弹解锁框 |
| 自动重连：`OnTabDisconnected` 定时器（`:2812-2832`）、`ReconnectAllAfterResume`（`:2694-2713`）、隧道 `AutoReconnectAsync` | **非交互式** | 抛 `Locked`，标签显示失败覆盖层，文案为「保险库已锁定，按 Ctrl+R 重连」 |
| 连接诊断 | 交互式（用户主动打开） | 弹解锁框 |
| 插件 `Sessions.OpenAsync` | 交互式（宿主本来就会弹确认框） | 弹解锁框 |

改动点：`ReconnectTabAsync` 增加参数 `bool interactive`，由各调用方传入；`TunnelPanelViewModel.AutoReconnectAsync` 传 `false`。

**理由**：网络恢复后若 20 个标签同时自动重连，交互式会立刻弹出一个解锁框。用户可能根本不在电脑前，回来看到一个不知由谁触发的解锁请求，这正是 §15 R9 担心的钓鱼疑虑。

### 5.8 失败回退接线（决策 5）

**五个重试循环的改法相同**：

```csharp
catch (CredentialProviderException ex) when (InteractiveAuthenticator is not null)
{
    pendingNotice = CredentialNotice.From(ex);   // 带到下一次弹窗
    tab?.MarkConnectionFailed(...);              // SSH 终端路径
    continue;                                    // attempt > 0 → 必然弹窗
}
```

- **登录弹窗**：`AuthenticationDialogViewModel` 构造函数新增 `CredentialNotice? notice = null` 与 `bool hideRememberPassword = false`；`InteractiveAuthenticator` 委托签名相应扩展（或增加一个带选项的重载）。
- **没有弹窗委托的路径**（例如隧道面板里无委托的情况、非交互重连）：把异常转成本地化的连接错误文案，走现有的失败覆盖层或气泡。
- **手输密码仅本次有效**：带引用的配置在登录弹窗里隐藏「记住密码」。即使弹窗结果带回 `RememberPassword = true`，仓储不变量也会剥掉密码。
- **服务器拒绝了保险库给的密码**（`VelaSshAuthenticationException` 等）：沿用原有的认证失败重试。弹窗提示条写「来自 {provider} 的密码被服务器拒绝，条目可能已过期」。下一次尝试用用户手输的密码，**不再查保险库**（优先级规则，§5.5）。

**提示条内容**：

| `CredentialFailure` | 文案要点 | 附加操作 |
|---|---|---|
| `NotInstalled` | 未找到 {provider} 命令行工具 | 「打开设置」→ 定位到凭据页 |
| `ProviderDisabled` | {provider} 已在设置中停用 | 「打开设置」 |
| `NotSignedIn` | {provider} 未登录（1Password：请在 App 的「设置 → 开发者」开启 CLI 集成；Bitwarden：请先在终端执行 `bw login`） | — |
| `Locked` / `UnlockCancelled` | 保险库未解锁 | 「重试解锁」 |
| `UnlockFailed` | 解锁口令错误 | 「重试解锁」 |
| `NotFound` | 条目已不存在（可能被删除、移动或重命名；KeePassXC 按路径寻址，改标题也会找不到） | 「重新选择条目」→ 打开配置页 |
| `Ambiguous` | 找到多个同名条目（KeePassXC） | 「重新选择条目」 |
| `MissingField` | 条目缺少用户名或密码 | — |
| `Timeout` | {provider} 响应超时（1Password 可能在等待 Windows Hello 确认） | 「重试」 |
| `Malformed` | {provider} 返回了无法识别的结果，可能是版本不兼容 | 「打开日志目录」 |

### 5.9 解析值在内存中的去向

| 载体 | 驻留 | 处理 |
|---|---|---|
| `ConnectionInfo` → `SshSession.ConnectionInfo` | 会话生命周期 | 修 D4 之后去掉机密 |
| `ConnectionInfo` → `TmdsSshClientWrapper._settings` | 会话生命周期 | Tmds.Ssh 内部，维持现状（与手输密码相同） |
| `FtpConnectionInfo` → `FtpConnectionPool` | 连接池生命周期 | **维持现状**：池子按需新开连接时要用密码。若改成每次重新解析，后台补连接时会触发解锁，得不偿失 |
| SDK 请求 DTO → 进程内插件 | 由插件决定 | 与今天手输密码的信任等级相同，在 [12 威胁模型](../plugins/12-security-threat-model.md) 中写明 |
| `tab.Profile` | 标签生命周期 | 引用配置的 `tab.Profile` 不含解析出的密码，重连会重新解析；手输密码的副本含密码（与今天一致） |

### 5.10 与同步、导入、导出等流程的交互

| 流程 | 行为 | 需要改代码吗 |
|---|---|---|
| Gist 推送（未加密） | 清空 `Password`，引用照常上传 | 否 |
| Gist 推送（端到端加密） | 引用配置在库里本来就没有 `Password` | 否 |
| Gist 拉取 | 远端带引用、本机有旧密码时，`:623` 会把密码拷回，仓储不变量随即剥掉 | 否 |
| Gist 拉取到一台没配置该 provider 的机器 | 连接时抛 `NotInstalled` 或 `ProviderDisabled`，退回弹窗手输 | 否 |
| 会话导入 | 导入器不产出引用 | 否 |
| 复制配置 | `Clone()` 复制引用，两条配置指向同一条目，符合预期 | 否（补 `Clone` 即可） |
| 外部拉起 | 请求不带密码时复用已存配置，已存配置带引用即可正常解析 | 否，回归测试一遍 |
| AI / 插件连接提议 | 提议带明文密码，走配置弹窗保存，不涉及引用 | 否 |
| 配置导出 / 导入 | 若 provider 设置放在 `AppSettings` 里，本机路径会被导出；按 §16 N4 放在 `AppSettings` 之外则不受影响 | 视 N4 而定 |

### 5.11 审计与日志

**审计**（`IAuditLogService`，`Category = "security"`）只记**失败与解锁事件**，避免每次连接都多写一条：

| Action | 时机 | Detail |
|---|---|---|
| `credential-failed` | 解析抛出 `CredentialProviderException` | `{providerId} {kind}`，**不写条目标题** |
| `vault-unlocked` | 解锁成功 | `{providerId} {targetName?}` |
| `vault-unlock-failed` | 解锁口令错误 | `{providerId}` |
| `vault-forgotten` | TTL 到期、手动锁定、网络恢复时清缓存 | `{providerId} {reason}` |

`ProfileId` 填写对应配置。写审计失败不影响连接，与 `RecordHistoryAsync` 的做法相同。

**诊断日志**：`Trace.WriteLine("[Cred] …")`，只记：

- provider id、子命令名（`item get` / `unlock` 等，**不记参数**）、耗时、退出码、`CredentialFailure` 种类；
- stderr 取前 200 个字符，并替换其中的邮箱、路径。

**永不记 stdout**。

### 5.12 连接诊断

- `HasStoredCredentials` 对带引用的配置返回 true。
- **优化建议**：诊断流程在第 4 步（认证）之前增加一步「凭据来源」。它调用 `GetStatusAsync` 报告 provider 状态，再以交互模式试解析，失败时给出 §5.8 那张表里的文案。这样用户排查「连不上」时，能分清是保险库的问题还是服务器的问题。步骤文案沿用 `DiagSvc_` 前缀。

---

## 6. 解锁会话缓存

### 6.1 各 provider 缓存什么

| provider | 缓存对象 | 说明 |
|---|---|---|
| 1Password | **不缓存** | 授权由桌面 App 管理：连续 10 分钟不用即失效，最长 12 小时；App 锁定时撤销全部授权（官方文档，§8.1） |
| Bitwarden | `bw unlock` 换回的 **session key** | 主密码只用一次，**不缓存主密码本身** |
| KeePassXC | **数据库口令**，按数据库别名分别缓存 | CLI 没有会话概念，每次调用都要口令 |
| 系统密钥链 | 不缓存 | 系统负责 |

### 6.2 规则

- **设置项** `UnlockCacheMinutes`：默认 **15**，`0` 表示不缓存（每次都问），上限 **480**（与 1Password 12 小时同一量级，再长就不合理了）。
- **绝对过期**：从解锁那一刻起算，不做滑动续期。滑动续期在持续使用时等于永不过期。
- **清除时机**：
  - TTL 到期（每个 provider 一个 `Timer`）；
  - 应用退出；
  - 设置页「立即锁定」按钮；
  - `bw status` 返回 `locked`（说明 session key 已失效）；
  - KeePassXC 口令被拒；
  - **`ConnectivityMonitor.Resumed`**：休眠唤醒时网卡会重新初始化并触发这个事件（§16 N6）。
- **承载与清零**：`char[]`（口令）或 `byte[]`（session key 的 UTF-8 形式）；清除时调用 `CryptographicOperations.ZeroMemory`。
- **诚实边界**：写进子进程 stdin 或 `ProcessStartInfo.Environment` 时，不可避免会生成托管 `string` 副本，GC 之前会留在堆上。这与 `SecureStringConvert.ToPlaintext` 交接点的取舍一致，在代码注释里写明，不做表面功夫。

---

## 7. CLI 运行器

`CliCredentialRunner`（`Infrastructure/Credentials/CliCredentialRunner.cs`）是三家 provider 共用的调用封装，全方案风险最集中的地方。

### 7.1 接口草案

```csharp
internal sealed record CliInvocation(
    string ExecutablePath,
    IReadOnlyList<string> Arguments,                          // 走 ArgumentList，绝不拼接字符串
    IReadOnlyDictionary<string, string?>? Environment,        // 只作用于子进程；值为 null 表示删除继承来的同名变量
    ReadOnlyMemory<char> StdinSecret,                         // 可空；写入后立即关闭 stdin
    TimeSpan Timeout,
    int MaxStdoutBytes = 4 * 1024 * 1024);

internal sealed record CliResult(int ExitCode, byte[] Stdout, string StderrRedacted, TimeSpan Elapsed);

internal sealed class CliCredentialRunner
{
    Task<CliResult> RunAsync(CliInvocation invocation, CancellationToken ct);
}
```

`Stdout` 用 `byte[]` 而不是 `string` 返回：调用方解析完即可清零，也便于直接交给 `JsonDocument.Parse(ReadOnlyMemory<byte>)`。

### 7.2 硬性规则

| # | 规则 | 理由 / 依据 |
|---|---|---|
| 1 | 可执行文件用**设置里保存的绝对路径**，调用时不查 PATH | 防 PATH 劫持 |
| 2 | 机密**不进命令行参数** | 参数会出现在进程列表里 |
| 3 | 传入机密走 **stdin**（KeePassXC）或**只写进子进程的环境变量**（Bitwarden 的 `--passwordenv` 与 `BW_SESSION`） | 同上 |
| 4 | **绝不调用** `Environment.SetEnvironmentVariable` | ConPTY 本地终端继承 VelaShell 整个进程环境（`ConPtyShellStream.cs:223-226`） |
| 5 | 显式删除继承来的敏感变量：Bitwarden 调用删除 `BW_SESSION`（再按需写入自己的）；1Password 调用**不改动** `OP_ACCOUNT` / `OP_BIOMETRIC_UNLOCK_ENABLED` / `OP_SERVICE_ACCOUNT_TOKEN` | 用户从带 `BW_SESSION` 的 shell 启动 VelaShell 时，不该悄悄借用那个会话；1Password 的环境变量是用户的显式配置，应当尊重 |
| 6 | `UseShellExecute = false`、`CreateNoWindow = true`、重定向 stdin / stdout / stderr | 避免 Windows 上闪出控制台窗口 |
| 7 | `StandardOutputEncoding = StandardErrorEncoding = UTF8`（无 BOM）；stdin 同样用 UTF-8 写入 | 条目标题、口令可能含非 ASCII 字符；Windows 控制台默认代码页会让它们乱码（**待实测**三家 CLI 在 Windows 上的实际输出编码） |
| 8 | 超时到达时 `process.Kill(entireProcessTree: true)`；取消令牌同样处理 | 参照 `PluginProcessClient.TryKill`（`:361-374`） |
| 9 | stdout 读满 `MaxStdoutBytes` 即截断并杀进程，报 `Malformed` | 防止异常输出撑爆内存 |
| 10 | stderr 只保留前 200 个字符，并替换邮箱与路径后才允许进日志 | §5.11 |
| 11 | **stdout 永不进日志、永不进异常消息** | 可能含密码 |
| 12 | 全程在线程池上执行，不占 UI 线程 | 1Password 的 Windows Hello 窗口由它自己的 App 弹出，我们只 await |
| 13 | 同一个 provider 的 CLI 调用串行执行（与 §5.6 解锁串行化共用一把锁，或单独一把锁） | `bw` 与 `keepassxc-cli` 并发访问同一数据目录或数据库的行为**待实测**；串行最稳 |
| 14 | 默认超时：交互式 90 秒（给 Windows Hello / YubiKey 触摸留时间），非交互式 20 秒，状态探测 5 秒 | 可在 provider 实现里覆盖 |

### 7.3 可执行文件定位

- **优先**用设置里保存的绝对路径；文件不存在则报 `NotInstalled`。
- **自动探测**（设置页「自动检测」按钮，或首次打开设置页时执行一次）：
  - 在 PATH 里查找，参照 `LocalShellCatalog.FindOnPath`（`:53-76`）。那里只在 Windows 上生效、且不处理 PATHEXT，这里要写成跨平台版本（Windows 依次试 `.exe` / `.cmd`，`bw` 用 npm 安装时是 `bw.cmd`，**待实测**）。
  - 再查各家常见安装目录（**待实测**后再写死，不凭记忆）。
  - 探测结果写回设置并显示出来，**由用户确认后才启用**，不静默启用。
- **签名校验（可选，不作为 v1 必做项）**：
  - 仓库目前没有 Authenticode 校验的先例。
  - Windows 上 1Password App 会自己校验连接方 `op.exe` 的 Authenticode 签名（官方文档），所以对 1Password 来说，VelaShell 侧的校验是重复劳动。
  - Bitwarden / KeePassXC 没有类似机制。若要做，Windows 用 `WinVerifyTrust`；其它平台暂不做。

### 7.4 JSON 解析

- 用 `JsonDocument` 按需取字段（参照 `HttpPluginMarketClient.cs:104,229` 的 DOM 写法），**不定义强类型模型**，以容忍 CLI 输出字段的增减。
- 解析完成后，把 stdout 字节数组清零。

---

## 8. 三家 provider 规格

### 8.1 1Password（`op` v2）

**已核实的事实**

- **App 集成**：在 1Password App 的「设置 → 开发者 → Integrate with 1Password CLI」开启；支持 Mac / Windows / Linux，Linux 需要 PolKit；Windows 上用 Windows Hello。
- **授权时长**：「连续 10 分钟不活动即过期」「硬上限 12 小时」「每次使用自动续期」。
- **授权作用域**：macOS / Linux 限定在一个终端会话内，并延伸到该窗口的子 shell；**「On Windows, commands executed in a sub-shell require separate authorization」**。
- **App 锁定**：「When the 1Password app is locked, all prior authorization is revoked」。
- **Windows 通信方式**：命名管道；App 会校验连接进程可执行文件的 Authenticode 签名。
- **`op item get`**：`op item get [{ <itemName> | <itemID> | <shareLink> | - }] [flags]`，支持 `--fields label=username,label=password` 与 `type=` 过滤、`--vault`、`--reveal`（人类可读输出里显示被隐藏的字段）、`--otp`、`--include-archive`。**官方建议按 ID 寻址**，可以避免限流和同名歧义。
- **`op item list`**：支持 `--vault`、`--categories`、`--tags`、`--favorite`、`--long`、`--include-archive`。**JSON 字段结构文档没有写明**。
- **多账户**：`--account` 标志，或 `OP_ACCOUNT` 环境变量。
- **`op whoami`**：「没有已认证的账户时返回错误」。
- **`OP_BIOMETRIC_UNLOCK_ENABLED`**：用于开关 App 集成。

**设计**

| 操作 | 命令 | 备注 |
|---|---|---|
| 状态探测 | `op --version`；再 `op account list --format json` | **不调用 `op whoami` 或 `item` 命令**，以免设置页一打开就弹 Windows Hello。`account list` 本身会不会触发授权**待实测**；若会，状态探测只做到 `--version` 为止，登录状态标为 `Unknown` |
| 搜索 | `op item list --categories Login --format json [--account <id>] [--vault <id>]` | 会触发授权（用户主动打开选择器，可以接受）。结果在本次选择器会话内缓存 |
| 补用户名 | 用户选中条目后执行 `op item get <itemId> --vault <vaultId> --account <accountId> --fields label=username --format json` | 列表 JSON 里有没有用户名没有文档，不依赖它 |
| 解析 | `op item get <itemId> --vault <vaultId> --account <accountId> --format json` | 取完整条目 JSON，**优先按字段的 `purpose`（`USERNAME` / `PASSWORD`）挑选**，找不到再按 `label` 回退。这样用户改过字段标签也能正确取值。`purpose` 字段是否存在**待实测**；JSON 输出里被隐藏的字段是否需要 `--reveal`**待实测**（社区反馈 JSON 直接含值），实测前一律带上 `--reveal` |

**失败映射**（错误文本**待实测**后补全匹配规则）：

| 情形 | `CredentialFailure` |
|---|---|
| 可执行文件不存在 | `NotInstalled` |
| 没有账户，或 App 集成未开启 | `NotSignedIn` |
| 用户在 Windows Hello 弹窗点了取消 | `UnlockCancelled` |
| 条目不存在或已归档 | `NotFound` |
| 超时 | `Timeout` |

**⚠️ 第一实测项：Windows 上的授权作用域**

官方只说「子 shell 需要单独授权」，没有说明由 GUI 进程拉起的 `op` 怎么算。可能的结果：

- (a) 以 VelaShell 进程为「终端会话」，10 分钟内多次调用只授权一次 —— 理想情况；
- (b) 每次调用 `op` 都要一次 Windows Hello —— 必须把一次连接压到**只调用一次** `op`（补用户名与解析合并成同一次 `item get`），选择器搜索也要缓存结果；
- (c) 其它情况。

实测结果写回本节，并据此决定是否要合并调用。

**要避开的坑**

- 用户环境里若有 `OP_SERVICE_ACCOUNT_TOKEN`，`op` 会改用服务账户，不走 App 集成。这是用户自己的配置，状态探测时应当识别出来并显示「服务账户模式」。
- 1Password 的 SSH Agent 属于私钥线，与本期无关。

来源：[item 命令参考](https://www.1password.dev/cli/reference/management-commands/item) · [App 集成](https://www.1password.dev/cli/app-integration) · [App 集成安全模型](https://www.1password.dev/cli/biometric-security) · [whoami](https://www.1password.dev/cli/reference/commands/whoami) · [CLI 发布说明](https://releases.1password.com/developers/cli/)

### 8.2 Bitwarden（`bw`）

**已核实的事实**

- **`bw status`** 返回 `{ serverUrl, lastSync, userEmail, userId, status }`。`status` 取值为 `"unlocked"` / `"locked"` / `"unauthenticated"`。调用前 CLI 会先尝试一次自动解锁，失败不抛异常。
- **全局参数**：`--raw`（原始输出）、`--response`（JSON 包装）、`--quiet`、`--nointeraction`（「Do not prompt for interactive user input」，内部会设置 `BW_NOINTERACTION=true`）、`--session <key>`（内部等价于设置 `BW_SESSION`）、`--cleanexit`、`--pretty`。
- **`bw unlock [password]`** 用于「Unlock the vault and return a new session key」，支持 `--check`、`--passwordenv <var>`、`--passwordfile <path>`。session key 在 `bw lock` 或 `bw logout` 之前一直有效。
- **`bw lock`** 用于「Lock the vault and destroy active session keys」。
- **`bw list items`** 支持 `--search`、`--folderid`、`--url`。**输出是完整条目，包含 `login.password`**。
- **`bw get item <id>`** 返回完整条目 JSON；另有 `bw get password <id>` / `bw get username <id>`。
- **`bw sync`**（`-f` 强制全量、`--last` 查看上次同步时间）；**`bw config server <url>`**。
- **登录方式**：邮箱密码（支持两步验证）、`--apikey`（`BW_CLIENTID` / `BW_CLIENTSECRET`）、`--sso`。
- **安装方式**：原生可执行文件（Windows / macOS / Linux x64）、npm、snap、Chocolatey。它是 Node.js 应用。

**设计**

| 操作 | 命令 | 备注 |
|---|---|---|
| 公共约定 | 每次调用都带 `--nointeraction` | stdin 被重定向时，CLI 若等待交互输入就会一直挂到超时 |
| 状态探测 | `bw status --nointeraction`（子进程环境里删掉 `BW_SESSION`） | 从 `status` 映射出 `NotSignedIn` / `Locked` / `Ready`；`userEmail` 用于解锁弹窗的 `TargetName` |
| 解锁 | `bw unlock --passwordenv VELA_BW_PW --raw --nointeraction`，`VELA_BW_PW` 只写进子进程环境 | stdout 就是 session key，放进 §6 的缓存。**不用 `--passwordfile`**（会落盘） |
| 搜索 | `bw list items --search <q> --nointeraction`，环境变量 `BW_SESSION=<key>` | ⚠️ **输出含密码**：流式解析成 `CredentialItemSummary`（`id`、`name`、`login.username`、`login.uris[0].uri`、`folderId`）后立即丢弃，stdout 字节数组清零。**选择器一次会话内只调用一次**，之后在本地过滤 |
| 解析 | `bw get item <id> --nointeraction`，`BW_SESSION=<key>` | 取 `login.username` / `login.password`；条目类型不是 login 时报 `MissingField` |
| 同步 | `bw sync --nointeraction` | 选择器里的「同步」按钮手动触发，**不自动同步** |
| 锁定 | **不调用 `bw lock`** | 它会销毁 CLI 数据目录中的所有会话，连用户自己终端里的 `BW_SESSION` 也一起失效。VelaShell 只丢弃内存中的 session key |

**失败映射**：

| 情形 | `CredentialFailure` |
|---|---|
| `unauthenticated` | `NotSignedIn`（登录不在本期范围，提示用户自己在终端执行 `bw login`） |
| `locked`，且处于非交互模式 | `Locked` |
| 解锁口令错误（错误文本**待实测**） | `UnlockFailed`，交互模式下再弹一次，最多 3 次 |
| `get item` 找不到 | `NotFound` |

**要避开的坑**

- 自行部署的服务器由用户自己执行 `bw config server` 配置，VelaShell 不代劳。
- 用 npm 安装时，Windows 上的入口是 `bw.cmd`，要经 `cmd.exe` 转一层（**待实测**）。确实如此的话，参数转义要格外小心，或者建议用户改装原生版。
- Node 应用每次启动都有开销，具体耗时**待实测**。选择器搜索要做防抖，并复用结果。

来源：[Bitwarden CLI 帮助](https://bitwarden.com/help/cli/) · [program.ts](https://github.com/bitwarden/clients/blob/main/apps/cli/src/program.ts) · [status.command.ts](https://github.com/bitwarden/clients/blob/main/apps/cli/src/commands/status.command.ts)

### 8.3 KeePassXC（`keepassxc-cli`）

**已核实的事实**

- **口令读取**：`Utils::getPassword` 用 `QTextStream` 从 **stdin 读一行**，读取前后切换回显；**管道输入同样可用**。提示文字「Enter password to unlock %1: 」写到 **stderr**，`-q` 时写到空设备。
- **数据库参数**：`-k/--key-file <path>`、`--no-password`、`-y/--yubikey <slot[:serial]>`（slot 只能是 1 或 2）、`-q/--quiet`。
- **`show`**：支持 `-a/--attributes <name>...`、`-s/--show-protected`、`--all`、`-t/--totp`、`--show-attachments`。
  - 带 `-a` 时**每行输出一个值**；
  - 属性名**区分大小写，并支持部分匹配** —— 有歧义时报错「ERROR: attribute %1 is ambiguous, it matches %2.」；
  - 不带 `-a` 时，受保护属性显示为 `PROTECTED`。
- **条目寻址**：`Group::findEntryByPath` **只接受路径，不支持 UUID**。标题重名时**返回第一个匹配项**。找不到时报「Could not find entry with path %1.」，退出码为 `EXIT_FAILURE`。
- **`ls`** 支持 `-R/--recursive`、`-f/--flatten`；**`search <database> <term>`** 的输出格式没有文档；**`open`** 进入交互式 shell 模式。
- **没有 JSON 输出**。

**设计**

| 操作 | 命令 | 备注 |
|---|---|---|
| 状态探测 | `keepassxc-cli --version`；再检查设置里每个数据库文件是否存在、是否可读 | 不打开数据库 |
| 搜索 | `keepassxc-cli ls -R -f -q [-k <keyfile>] [-y <slot>] <db>`，口令经 stdin 传入 | 输出扁平化的条目路径（分组以 `/` 结尾，**待实测**具体格式），在本地过滤。结果**只含路径，不含机密**，在该数据库的解锁会话内缓存 |
| 补用户名 | 选中后执行 `show -q -a UserName <db> <path>` | 或者在搜索阶段不显示用户名，以减少 KDF 次数（见下文「性能」） |
| 解析 | `keepassxc-cli show -q -s -a UserName -a Password [-k] [-y] <db> <entryPath>`，口令经 stdin 传入 | 第一行是用户名，**其余行拼起来是密码**（防止密码里本身带换行）。`-s` 与 `-a` 同时使用在各版本上的行为**待实测** |
| 选择时查重 | 同一路径在 `ls` 结果里出现不止一次时，选择器提示「存在同名条目，可能取到其它条目」，并建议用户先在 KeePassXC 里改名 | 因为 CLI 按路径取第一个匹配项 |

**失败映射**：

| 情形 | `CredentialFailure` |
|---|---|
| 数据库文件不存在或不可读 | `NotInstalled`，文案改为「数据库文件不可用」 |
| 数据库打开失败（口令错误时只有笼统的打开错误，**待实测**具体文本） | `UnlockFailed` |
| 「Could not find entry with path」 | `NotFound` |
| 「is ambiguous」 | `Malformed`，并提示是属性名冲突 |
| 触摸 YubiKey 超时 | `Timeout` |

**性能：每次调用都要重新派生密钥**

每次调用 CLI 都会重新执行一次 KDF。KeePass 数据库默认的 Argon2 参数就是**有意设计得耗时**，具体耗时取决于数据库设置，**待实测**。于是一次「搜索 + 补用户名 + 解析」就要执行三次 KDF。两个方案：

- **v1（推荐）**：逐次调用，但尽量合并 —— 解析只调用一次 `show`（同时取两个属性），选择器里只调用一次 `ls`，用户名只对选中的那一条去取。
- **后续优化（§16 N8）**：每个解锁会话保持一个 `keepassxc-cli open` 交互进程，经 stdin 发送命令，只做一次 KDF。代价是数据库在子进程里一直保持解锁、交互模式的输出更难解析，而且进程常驻。等 v1 实测证明确实慢到影响使用再做。

来源：[keepassxc-cli 手册](https://github.com/keepassxreboot/keepassxc/blob/develop/docs/man/keepassxc-cli.1.adoc) · [Utils.cpp](https://github.com/keepassxreboot/keepassxc/blob/develop/src/cli/Utils.cpp) · [Show.cpp](https://github.com/keepassxreboot/keepassxc/blob/develop/src/cli/Show.cpp) · [Group.cpp](https://github.com/keepassxreboot/keepassxc/blob/develop/src/core/Group.cpp)

---

## 9. 系统密钥链 provider

- **阶段 2**，依赖[密钥链调研](系统密钥链与sudo凭据填充可行性调研.md)阶段 0/1 的 `IMasterKeyStore` P/Invoke 层。
- **Windows 先行**：`CredEnumerateW` 做搜索（按 TargetName 过滤），`CredReadW` 读取 `CRED_TYPE_GENERIC` 条目，`ItemId` 为 TargetName，用户名取 `UserName`，密码取 `CredentialBlob`（UTF-16，**待实测**是否所有写入方都用 UTF-16）。
- 不需要 CLI 运行器，也没有解锁缓存。
- macOS（`SecItemCopyMatching`）与 Linux（libsecret）的实施细节，与调研文档 §2 的平台层一并确定。

---

## 10. 设置与界面

### 10.1 设置存放在哪里

**建议（§16 N4）：provider 设置不放进 `AppSettings`，单独存一份本机文档**，形式参照 `SyncSettings`（`Core/Sync/SyncModels.cs`）。

| | 放进 `AppSettings` | **单独的本机文档（推荐）** |
|---|---|---|
| 可执行文件路径、数据库路径这类本机字段 | 要同时加进 `ScrubDeviceLocalFields` 和 `PreserveDeviceLocalFields`，漏一边就会跨设备串路径 | 天然不参与同步 |
| 配置导出 | 路径会随导出文件带走 | 不涉及 |
| 设置页的加载与保存 | 要在 `ApplyToViewModel` 和 `SaveAsync` 两处都加（`SettingsViewModel.cs:1454-1495`、`:1674-1712`） | 设置页 VM 自己读写，参照 `SyncPage` 的做法 |
| TTL 偏好跨设备漫游 | 可以 | 不能（影响很小） |

文档结构（SonnetDB 文档集合 `app_config`，id 为 `credential_providers`；**待实施时确认**集合与命名约定）：

```
CredentialProviderSettings
├─ UnlockCacheMinutes : int = 15          （0 ~ 480，Normalize 时钳位）
├─ OnePassword  { Enabled, ExecutablePath, DefaultAccountId? }
├─ Bitwarden    { Enabled, ExecutablePath }
└─ KeePassXc    { Enabled, ExecutablePath,
                  Databases: [{ Alias, Path, KeyFilePath?, YubiKeySlot? }] }
```

- 数据库**别名**一旦被某条引用使用，改名时要提示「有 N 条配置引用了这个别名」。
- 删除数据库条目前，要求先确认。
- 所有字段都要有运行时读取方，不新增「存了但不生效」的开关；同步更新 [settings-audit.md](settings-audit.md)。

### 10.2 设置页

**建议（§16 N5）：新增独立设置页「凭据管理器」**，不挤进「常规 → 隐私与安全」。原因是内容量大：三个 provider 行，加上 KeePassXC 数据库列表，再加 TTL 与「立即锁定」。

新增设置页要改的地方（照现有模式）：

1. `enum SettingsSectionKey` 增加成员（`SettingsViewModel.cs:51-88`，**枚举顺序就是导航顺序**，建议放在「安全审计」后面）；
2. `BuildSections()` 增加一项（`:495-545`），导航文案键为 `SetVm_SectionCredentials`；
3. `SettingsPageHost.Create` 增加一个 switch 分支（`SettingsPageHost.cs:52-69`）；
4. 更新 `tests/VelaShell.Tests/ViewModels/SettingsSectionKeyTests.cs:26-47` 中硬编码的页面顺序；
5. 以下测试会自动覆盖新页面，要确保通过：`SettingsLazyPagesUiTests`（页面数等于枚举数）、`SettingsBindingErrorTests`（不能有绑定错误）、`AccessibleNameTests`（每个按钮都要有无障碍描述）。

**页面布局**：

```
凭据管理器
  从密码管理器读取连接密码。VelaShell 不保存这些密码，只保存指向条目的引用。

  1Password                         ● 已就绪 · 2.x.x        [启用 ◯]
    命令行路径  C:\…\op.exe                [浏览…] [自动检测]
    默认账户    ▾ my.1password.com
  Bitwarden                         ● 已锁定 · 202x.x.x     [启用 ◯]
    命令行路径  …                          [浏览…] [自动检测]   [立即锁定]
  KeePassXC                         ● 已安装 · 2.7.x        [启用 ◯]
    命令行路径  …                          [浏览…] [自动检测]   [立即锁定]
    数据库
    ┌ 别名 ─────┬ 路径 ─────────────┬ 密钥文件 ┬ YubiKey ┬─────┐
    │ work      │ D:\kp\work.kdbx    │ —        │ 2       │ [删除]│
    └───────────┴────────────────────┴──────────┴─────────┴─────┘
    [添加数据库…]

  解锁状态保留  [ 15 ] 分钟（0 = 每次都询问）
```

- **状态探测**：进入页面时异步执行，写法参照「关于」页检查更新的 `UpdateStatus`（`SettingsViewModel.cs:779-855`）。**不在应用启动时探测**。
- **状态显示**：沿用 `StatusDot`（DESIGN.md §5.2）；不要新增颜色字面量。
- **数据库表格**：参照 `KeyManagementPage.axaml:16-88` 的 `Border.table` 加 `ItemsControl`。目前**没有**就地编辑列表的先例，所以「添加数据库」做成弹出小表单，不做表格内编辑。
- **文件选择**：沿用 `StorageProvider.OpenFilePickerAsync` 的写法（`AppearanceSettingsPage.axaml.cs:17-37`），`.kdbx` 过滤器。

### 10.3 连接配置页

改动位置在 `ConnectionProfileView.axaml` 与 `ConnectionProfileViewModel.cs`：

| 区域 | 改动 |
|---|---|
| 密码区（`:469-492`，受 `ShowPasswordField` 控制） | 上方加一个「密码来源」分段选择：**手动输入 / 密码管理器**。分段样式复用 `AuthenticationDialogView.axaml:60-74` 的 `seg-tab`（建议顺手把这组样式提到公共主题里，§16 N10） |
| 选「密码管理器」时 | 隐藏密码输入框与「记住密码」勾选框（`:531-546`）；显示条目卡片：provider 图标、条目标题快照、用户名快照、「更换条目」按钮 |
| 高级区（`:566-674`，`IsAdvancedVisible`） | 新增「用本配置的用户名覆盖条目用户名」勾选框。勾选时，基础区的用户名输入框才可编辑；不勾选时显示条目用户名快照（只读） |
| 高级区徽标 `AdvancedBadge` | 覆盖用户名算作一个高级项 |
| 切换来源 | 从「手动」切到「密码管理器」、且已有密码时，用 `MessageDialog.ConfirmAsync` 确认「将清除已保存的密码」；从「密码管理器」切回「手动」时清空引用 |
| `BuildProfile()`（`:1265-1317`） | 写入 `CredentialSource`；来源为密码管理器时 `Password = null` |
| 加载（`:143` 附近） | 读取 `existing.CredentialSource`，决定分段选择的初始状态 |
| 只在密码认证时出现 | 私钥 / 证书认证（`:496-529`）不显示来源选择（决策 2） |
| 插件协议 | 插件可能改写密码标签（例如 S3 的 Secret Access Key），来源选择照常显示，标签跟随 `PasswordLabel` |
| FTP 匿名登录 | 与今天一样隐藏整个密码区 |
| 子对话框 | 「更换条目」经委托打开选择器（参照 `CopyToClipboard` 的注入写法，`.axaml.cs:226`） |

### 10.4 条目选择器（新对话框）

```
 选择凭据条目                                                   ✕
 ┌──────────────┐ ┌───────────────────────────────────┐
 │ 1Password  ▾ │ │ 🔍 搜索标题、用户名、网址            │  [同步]
 └──────────────┘ └───────────────────────────────────┘
 ┌──────────────────────────────────────────────────────┐
 │ prod-db-root          root         Private / DB     │
 │ bastion-01            ops          Infra            │
 │ ⚠ web (2 个同名)      deploy       /Servers          │   ← KeePassXC 重名提示
 └──────────────────────────────────────────────────────┘
                                          [取消]  [选择]
```

- **搜索框**参照 `CommandPaletteView.axaml:80-100`，**表格**参照 `KeyManagementPage`。
- provider 下拉框只列出**已启用且已探测到可执行文件**的 provider；KeePassXC 选中后，再出现一个数据库下拉框。
- **首次打开**时，按该 provider 的方式拉一次列表（1Password `item list`、Bitwarden `list items`、KeePassXC `ls`），之后**在本地过滤**，输入防抖 150 毫秒。
- 「同步」按钮只对 Bitwarden 显示。
- 选择器是用户显式打开的对话框，**允许获得焦点**，与调研文档 §4「提示条不抢焦点」的规矩不冲突。
- 需要解锁时，经 `ICredentialUnlockPrompt` 弹出解锁框，与连接时是同一个。
- 返回值是 `CredentialReference`（含标题与用户名快照）；取消则返回 null。

### 10.5 登录弹窗提示条

- **位置**：`AuthenticationDialogView.axaml:111-133` 信息栏的下方，或者直接替换信息栏的第二行。
- **样式**：参照 `HostKeyPromptView.axaml:113-139` 的警告条（`VelaBgActive` 背景 + `triangle-alert` 图标 + `VelaWarning` 文字）。DESIGN.md 里没有 `*Dim` 的警告色，**不要新造颜色**。
- **内容**：§5.8 表格中的文案，外加至多一个附加操作按钮；按钮要有无障碍描述。
- **带引用的配置**：隐藏「记住密码」相关控件（`:268-280` 的 AES 徽标一并隐藏）。
- **优化建议（§16 N10）**：仓库里目前有五六处各写各的提示条（§2.7 所引报告），可以借这次机会提炼出一个共用的 `Border.notice` 样式，并补进 DESIGN.md。不做也不影响本功能。

### 10.6 解锁弹窗（新对话框）

- **三件套**参照 `IHostKeyPrompt` → `HostKeyPromptDialogService`（`src/VelaShell/Services/HostKeyPromptDialogService.cs:15-49`）→ `HostKeyPromptView`：
  - `CredentialUnlockDialogService : ICredentialUnlockPrompt`，用 `Dispatcher.UIThread.InvokeAsync` 切到 UI 线程；
  - 从 `IClassicDesktopStyleApplicationLifetime.MainWindow` 取主窗口作为所有者；
  - **拿不到主窗口或出现异常时返回 null**（fail-closed）。
- **注册**：在 `App.axaml.cs:136` 附近加 `.AddSingleton<ICredentialUnlockPrompt, CredentialUnlockDialogService>()`；Infrastructure 里用 `sp.GetService<ICredentialUnlockPrompt>()` 可选解析。解析不到时，交互式解析一律报 `Locked`。
- **输入框**：复用 `AuthenticationDialogView.axaml:231-255` 的 `SecurePasswordBox` + `input-affix` + 显示密码按钮。
- **弹窗内容**（防钓鱼，§15 R9）：
  - 标题「解锁 {provider}」；
  - 目标：数据库别名或账户邮箱；
  - 原因：「为了连接 {连接显示名}」（**不带用户名与端口**，遵守 `ProfileDisplayName` 的约定）；
  - 上一次口令错误时显示错误提示。
- **出现时机**：**只在用户发起的操作链路里出现**（§5.7），不会在后台自己弹出。
- **行为**：Esc 等同取消；关闭后焦点还给调用它的窗口。

### 10.7 本地化

| 前缀 | 用途 | 示例 |
|---|---|---|
| `Cred_` | 服务层文案、失败提示条 | `Cred_Failure_Locked`、`Cred_Failure_NotFound` |
| `CredUnlock_` | 解锁弹窗 | `CredUnlock_Title`、`CredUnlock_Reason` |
| `CredPicker_` | 条目选择器 | `CredPicker_SearchPlaceholder`、`CredPicker_DuplicateWarning` |
| `SetCred_` | 设置页 | `SetCred_UnlockMinutes`、`SetCred_LockNow` |
| `SetVm_SectionCredentials` | 设置导航 | — |
| `Profile_CredSource*` | 配置页 | `Profile_CredSourceManual`、`Profile_CredSourceVault`、`Profile_CredOverrideUsername` |
| `Auth_CredNotice*` | 登录弹窗 | `Auth_CredNoticeRejected` |
| `DiagSvc_Cred*` | 连接诊断的新步骤 | — |

注意事项：

- **五份 resx 必须同时补齐**（`Strings` / `zh-Hans` / `zh-Hant` / `ja` / `ko`），`LocalizationTests.AllCultures_HaveIdenticalKeySets` 会检查。
- `CredentialFailure` 到文案键的映射**写成显式 `switch`，每个分支一个字面量键**，不要用 `$"Cred_Failure_{kind}"` 拼接。否则 `UnusedLocalizedKeyTests` 会把这些键判为孤儿，还得去改它的 `ComposedKeyPrefixes`（`UnusedLocalizedKeyTests.cs:31-35`）。
- `Strings.Format("Key", …)` 用到的键**不会**被 `LocalizedKeyUsageTests` 检查，拼写错误要靠单元测试兜住。

---

## 11. 范围外

| 项 | 为什么不在本期 | 去向 |
|---|---|---|
| 私钥 / 证书认证从保险库取 | 决策 2：走 SSH Agent | Agent 功能等上游 PR 合并后单独立项；届时同步修订 `plan.md` §17-A（当初刻意排除了 `SshAgentCredentials`） |
| 本地私钥文件的口令从保险库取 | 与私钥走 Agent 的方向重叠，需求待确认 | §17 |
| 插件形态的凭据提供方 | 决策 7 | 按 [15 §2.4](../plugins/15-ecosystem-ideas.md) 后置 |
| Bitwarden 登录与两步验证 | 交互复杂，而且是一次性操作，用户在终端完成即可 | 需求出现再评估 |
| sudo 提示处填充 | 属于调研文档的阶段二 | 复用本期的 `SearchAsync` / `ResolveAsync` |
| TOTP / 验证码 | 与 keyboard-interactive 认证相关，是另一条线 | 需求出现再评估 |
| 保险库条目变更后的后台巡检 | 会形成定时外呼，违背 `PRIVACY.md` 的立场 | 不做；靠 `NotFound` 提示条兜底 |
| 代理密码（`ProxyOptions.Password`）从保险库取 | 不是连接配置，属于另一条保存链路 | 需求出现再评估 |
| 系统锁屏事件监听 | 仓库刻意不引用 `SystemEvents`（§16 N6） | 以 `ConnectivityMonitor.Resumed` 与 TTL 代替 |

---

## 12. 分期与任务清单

### 阶段 0a · 修复既有缺陷（独立 PR，建议先合）

- [ ] **D1 / D3**：`MainWindow.axaml.cs` 中的 `PromptCredentialsCoreAsync` 改为在 `profile.Clone()` 上写入并返回副本
- [ ] **D1 / D3**：四个文档路径失败卡片的「重试」闭包（`MainWindowViewModel.cs:3492,3657,3787,4069`）改为捕获最新的 `current`
- [ ] **D1 / D3**：SSH 终端循环中 `tab.Profile = current`（`:3286`）的语义保持不变，并补一段注释
- [ ] **D1**：`PersistProfileIfSavedAsync`（`:4509-4526`）改为经 workflow 的剥离逻辑保存。可以新增 `IConnectionWorkflowService.SaveProfileMetadataAsync`，或复用 `SaveProfileAsync`
- [ ] **D2**：按 §16 N2 的结论，补上口令剥离或补注释
- [ ] **D4**：`SshConnectionService.ConnectInternalAsync` 给 `SshSession` 挂一份去掉机密的副本（递归处理 `JumpHost`），并确认没有读取方依赖 `Password`
- [ ] 测试：弹窗不修改传入实例；「记住密码」未勾时，经拖动分组、复制配置、证书信任三条路径都**不会**落盘手输密码（用内存仓储 spy 断言）；`SshSession.ConnectionInfo.Password` 为 null
- [ ] `plan.md` 补一节，写明修复内容

### 阶段 0b · 地基（用户无感）

- [ ] 新建 `Core/Credentials/`：§5.2 的全部契约、`CredentialReference`、`CredentialNotice`
- [ ] `SessionProfile.CredentialSource`，补 `Clone()` 与 `SessionProfileCloneTests.FullyPopulated()`
- [ ] `ModelSerializationTests` 补一条：老数据没有该字段时为 null；新数据往返序列化后保持一致
- [ ] 仓储不变量（§5.4），加测试
- [ ] `CredentialResolver`：provider 查找、单次尝试缓存、按 provider 串行解锁、取消后排队请求立即结束、用户名规则
- [ ] 五个构建点接线（§5.5），加优先级规则
- [ ] `ConnectProfileAsync` / `TestConnectionAsync` 增加 `CredentialResolveOptions`；`ReconnectTabAsync` 增加 `interactive` 参数，按 §5.7 逐个调用方传值；`TunnelPanelViewModel.AutoReconnectAsync` 传 `false`
- [ ] 六个判定点（§2.3）识别引用
- [ ] 五个重试循环加 `CredentialProviderException` 分支（§5.8）；`AuthenticationDialogViewModel` 增加 notice 与 `hideRememberPassword` 参数（提示条界面放到 1c）
- [ ] `ConnectionDiagnosticsService.HasStoredCredentials` 识别引用
- [ ] DI：`ICredentialResolver` 注册；不注册任何 provider 时行为与今天完全一致
- [ ] `SecureStringConvert` 下沉到 Core（§16 N7）
- [ ] 测试：§13.1 的全部不变量

### 阶段 1a · 运行器与解锁组件

- [ ] `CliCredentialRunner`（§7），全部硬性规则逐条对应单元测试
- [ ] 解锁会话缓存（§6）：TTL 计时器、清零、`ConnectivityMonitor.Resumed` 订阅、「立即锁定」
- [ ] `CredentialProviderSettings` 本机文档读写（§10.1），`Normalize` 钳位
- [ ] `ICredentialUnlockPrompt` + `CredentialUnlockDialogService` + 解锁弹窗（§10.6）
- [ ] 审计事件（§5.11）
- [ ] 测试夹具：假 CLI（§13.3）

### 阶段 1b · 三家 provider（**开工前先完成 §8 各节的「待实测」项**）

- [ ] **实测清单**，结果写回 §8：
  - 1Password：Windows 授权作用域、`account list` 是否触发授权、`purpose` 字段、JSON 输出是否需要 `--reveal`、错误文本
  - Bitwarden：`bw.cmd` 调用方式、解锁口令错误的文本、启动耗时
  - KeePassXC：`ls -f` 输出格式、`-s` 与 `-a` 组合、打开失败的文本、KDF 耗时、Windows 上的输出编码
- [ ] `OnePasswordCliProvider`
- [ ] `BitwardenCliProvider`
- [ ] `KeePassXcCliProvider`
- [ ] DI 按「先注册具体类型，再转发」的方式注册三家（参照 `:70-81`）
- [ ] 每家输出解析各自的单元测试（录制并脱敏后的样本入库）

### 阶段 1c · 界面

- [ ] 设置页「凭据管理器」（§10.2），含四处注册改动与测试更新
- [ ] 连接配置页的来源切换、条目卡片、覆盖用户名（§10.3）
- [ ] 条目选择器（§10.4）
- [ ] 登录弹窗提示条（§10.5）
- [ ] 连接诊断新增「凭据来源」步骤（§5.12）
- [ ] 五份 resx
- [ ] headless UI 测试（注意：`Dispatch` 要用带返回值的重载）
- [ ] 文档同步（§14）

### 阶段 2 · 系统密钥链 provider

- [ ] 等 `IMasterKeyStore` 的 P/Invoke 层就位
- [ ] Windows `CredEnumerateW` / `CredReadW` provider（§9）
- [ ] macOS / Linux 与调研文档一并确定

### 并行线与独立线

- **并行线** · `IMasterKeyStore`：调研文档阶段 0/1，与本文互不依赖。
- **独立线** · SSH Agent：私钥认证，等上游 PR 合并。

---

## 13. 测试计划

### 13.1 阶段 0a / 0b 必须钉住的不变量

| # | 不变量 | 测试位置 | 手段 |
|---|---|---|---|
| I1 | 带引用的配置，经任何保存路径落盘后 `Password` 都为空 | `VelaShell.Infrastructure.Tests`（仓储） | 真实 SonnetDB（参照 `SonnetDbPersistenceTests`） |
| I2 | 解析出的明文永远到不了 `SaveSessionAsync` | `VelaShell.Presentation.Tests`（workflow） | NSubstitute 替身仓储，断言收到的 profile 中 `Password` 为空 |
| I3 | 同步载荷（加密与未加密两种）不含解析出的明文 | `VelaShell.Infrastructure.Tests/Sync` | 参照 `GistSyncServiceTests` |
| I4 | resolver 抛异常时，发出去的不是引用字符串，而是走弹窗；没有弹窗委托时报连接错误 | `VelaShell.Tests`（`MainWindowViewModel`） | 替身 resolver，断言 `InteractiveAuthenticator` 被调用、`ConnectProfileAsync` 未被以错误密码调用 |
| I5 | 跳板链每一跳各自解析；跳板解析失败时不弹登录弹窗 | Presentation.Tests | 替身 resolver 记录调用 |
| I6 | 同一次尝试内的认证重试不再调用 provider；手输密码优先 | Presentation.Tests / VelaShell.Tests | — |
| I7 | 并发 N 个解析命中同一个锁定的 provider，只触发一次解锁；用户取消后其余请求立即结束 | Infrastructure.Tests（`CredentialResolver`） | 替身 `ICredentialUnlockPrompt` 计数 |
| I8 | 非交互模式绝不调用 `ICredentialUnlockPrompt` | 同上 | — |
| I9 | 登录弹窗不修改传入实例（D3） | VelaShell.Tests（headless） | 前后引用比对与字段比对 |
| I10 | 「记住密码」未勾时，拖动分组、复制配置、证书信任都不落盘手输密码（D1） | VelaShell.Tests | 内存仓储 spy |
| I11 | `SshSession.ConnectionInfo` 不含机密（D4） | Infrastructure.Tests（`SshConnectionService`，替身 client 工厂） | — |
| I12 | 所有连接判定点识别引用 | 各自所在项目 | `DataRow` 覆盖三种 AuthMethod × 有无引用 × 是否覆盖用户名 |

### 13.2 CLI 运行器

逐条对应 §7.2 的硬性规则：

- 参数里不出现传入的机密；
- 调用结束后，当前进程环境里不存在 `BW_SESSION` 或 `VELA_BW_PW`；
- 继承来的 `BW_SESSION` 在子进程里被删除；
- stdin 写入后被关闭；
- 超时会杀掉整个进程树（假 CLI 会再起一个子进程，断言两者都被结束）；
- stdout 超过上限时截断并报 `Malformed`；
- stderr 中的邮箱与路径被替换；
- 非 ASCII 字符往返不变。

### 13.3 假 CLI 夹具

- **新建** `tests/fixtures/VelaShell.FakeCredentialCli`：一个控制台程序，按环境变量 `VELA_FAKE_CLI_SCRIPT` 指向的 JSON 剧本，模拟 `op` / `bw` / `keepassxc-cli` 的输出、退出码、延迟、读取 stdin、再起子进程等行为。
- **引用方式**：参照现有夹具 —— 测试项目以 ProjectReference 引用，产物落在测试 bin 目录，测试里用 `typeof(...).Assembly.Location` 定位（`tests/fixtures/README.md`）。
- 三家 provider 的**流程测试**（状态探测 → 解锁 → 搜索 → 解析 → 失败映射）全部跑在假 CLI 上，不依赖真实安装。

### 13.4 输出解析样本

- 每家把实测时录下的真实输出**脱敏**后存进 `tests/VelaShell.Infrastructure.Tests/Credentials/Samples/`，文件名带 CLI 版本号。
- CLI 升级、输出变化时补充新样本，**不删除旧样本**，保证兼容。

### 13.5 真实 CLI 集成测试

- 环境变量门控：`VELASHELL_OP_TESTS=1` / `VELASHELL_BW_TESTS=1` / `VELASHELL_KPXC_TESTS=1`，未设置时 `Assert.Inconclusive(...)`（参照 `CrossPlatformPublishTests.cs:92-105`）。
- KeePassXC 可以在仓库里放一个测试用 `.kdbx`（口令写在测试代码里，数据库内容全是假数据），CI 只要装了 `keepassxc-cli` 就能跑。
- 1Password 与 Bitwarden 需要真实账户，**只在本地手动跑**，结果记录进 `plan.md`。

### 13.6 UI 测试

- 设置页：新页面自动纳入 `SettingsLazyPagesUiTests`、`SettingsBindingErrorTests`、`AccessibleNameTests`。
- 配置页：切换来源时，密码框、「记住密码」、条目卡片的显隐；切换时的确认对话框；`BuildProfile` 的输出。
- 登录弹窗：提示条显示与附加操作；带引用时隐藏「记住密码」。
- 解锁弹窗：取消返回 null；Esc 等同取消；主窗口缺失时 fail-closed。
- headless 测试一律使用 `Dispatch(async () => { …; return true; })`。

---

## 14. 文档与仓库同步清单

| 位置 | 改动 | 时机 |
|---|---|---|
| 本文 | 实施中结论有变就地修订；实测后去掉「待实测」标注；§18 追加修订记录 | 各阶段 |
| `en/host/` | 本文暂为中文独有，已登记在 `en/host/README.md` 与根 README 的清单里；翻译另行补齐 | — |
| [12 安全与威胁模型 §1](../plugins/12-security-threat-model.md) | 补充：凭据会在 CLI 子进程管道中短暂出现；解锁会话按 TTL 在内存中保留；插件协议拿到的密码可能来自保险库 | 阶段 1 |
| [settings-audit.md](settings-audit.md) | 登记 `CredentialProviderSettings` 的各字段及其读取方 | 阶段 1c |
| 宿主 `SECURITY.md` | 范围增加「凭据管理器集成：CLI 调用、解锁会话缓存」 | 阶段 1 |
| 宿主 `PRIVACY.md` | 说明 VelaShell 自身不外呼，但 `bw sync` 等 CLI 会自行联网；引用元数据（条目标题快照、用户名快照）随 Gist 同步 | 阶段 1 |
| 宿主 README（中英） | 「未开放」一行更新 | 阶段 1 |
| 宿主 `plan.md` | 阶段 0a、各阶段完成后各补一节 | 各阶段 |
| 宿主 `feature-plan.md` | 完成后划掉本条；§3 的缺陷若决定不修，登记进待办 | 各阶段 |
| 宿主 `DESIGN.md` | 若采纳 §16 N10，补充共用提示条与分段控件的样式规范 | 阶段 1c |
| 快捷键参考 | 本功能不新增快捷键；若新增，先改 `ShortcutCatalog` | — |

---

## 15. 风险登记

| # | 风险 | 可能性 | 影响 | 缓解 |
|---|---|---|---|---|
| R1 | 1Password 在 Windows 上每次调用 `op` 都要一次 Windows Hello | 中 | 高：体验很差 | 阶段 1b 第一个实测；一次连接只调用一次 `op`；选择器结果缓存 |
| R2 | CLI 输出格式随版本漂移 | 高 | 中 | `JsonDocument` 按需取字段；样本测试；设置页显示版本号；`Malformed` 指明是哪一步出的问题 |
| R3 | `bw list items` 带出的密码驻留内存或进日志 | 中 | 高 | 流式解析后立即丢弃；stdout 永不进日志；有专门测试 |
| R4 | 敏感环境变量漏进本地终端 | 低（有规则） | 高 | §7.2 第 4、5 条；有测试；代码注释锁住 |
| R5 | KeePassXC 按路径寻址，重名时取错条目 | 中 | 高：用错密码连错机器，或被服务器拒绝 | 选择时查重并提示；`NotFound` / 被拒时引导重新选择 |
| R6 | KeePassXC 每次调用都重新派生密钥，导致很慢 | 中 | 中 | 合并调用；必要时做 §16 N8 的常驻进程方案 |
| R7 | 引用失效是静默的 | 高 | 低 | `NotFound` 提示条 +「重新选择条目」；不做后台巡检 |
| R8 | 跨设备同步后，另一台机器没有对应 provider | 高 | 低 | `NotInstalled` 退回弹窗手输，不阻塞连接 |
| R9 | 解锁弹窗被当成钓鱼载体，或让用户起疑 | 低 | 中 | 只在用户发起的操作链路里出现；显示 provider、目标与原因；自动重连不弹框 |
| R10 | 自动重连风暴触发大量 CLI 调用 | 中 | 中 | 非交互模式在锁定时快速失败；按 provider 串行化 |
| R11 | 阶段 0a 修复改变了用户习惯（例如失败卡片重试是否要重输） | 低 | 低 | 闭包捕获最新的 `current`，维持现状 |
| R12 | `bw lock` 误伤用户自己的 CLI 会话 | — | 中 | 明确不调用（§8.2） |
| R13 | 进程内插件拿到的是保险库密码 | — | 中 | 与今天手输密码的信任等级相同；在威胁模型中写明 |

---

## 16. 新增待确认决策

以下是 v2 梳理中出现的新问题。每项都给了建议，**请你逐条确认**。

| # | 问题 | 选项 | 建议 | 理由 |
|---|---|---|---|---|
| N1 | §3 的既有缺陷怎么处理 | A. 作为阶段 0a 先修 / B. 并入阶段 0b / C. 只登记，不修 | **A** | D1 违背用户的明确选择，与本功能无关，也值得单独修；先修能让 0b 建在干净的地基上 |
| N2 | 私钥口令是否受「记住密码」控制（D2） | A. 受控制，补上剥离 / B. 不受控制，只补注释 | **B**，并在配置页口令输入框旁加一行说明 | 配置页的「记住密码」只在密码认证时出现，口令一直是随配置保存的；改成受控制会让已有用户的口令在下次连接后消失 |
| N3 | 自动重连遇到保险库锁定 | A. 非交互，快速失败 / B. 照常弹解锁框 | **A** | §5.7；避免无人值守时弹框 |
| N4 | provider 设置放在哪里 | A. 单独的本机文档 / B. `AppSettings` | **A** | §10.1；本机路径天然不参与同步与导出 |
| N5 | 设置入口 | A. 新增独立设置页 / B. 放进「常规 → 隐私与安全」 | **A** | 内容量大；新增页面的改动点与测试都很明确 |
| N6 | 休眠或锁屏时清除解锁缓存 | A. 复用 `ConnectivityMonitor.Resumed` / B. 引入 `SystemEvents`，推翻既有取舍 / C. 只靠 TTL | **A** | 零新依赖，唤醒场景已覆盖；1Password 自己会在 App 锁定时撤销授权 |
| N7 | `SecureStringConvert` | A. 下沉到 Core / B. 在 Infrastructure 另写一份 | **A** | 它只依赖 BCL，与界面无关；两份实现迟早不一致 |
| N8 | KeePassXC 调用方式 | A. v1 逐次调用、合并参数，慢了再优化 / B. 一开始就做 `open` 常驻进程 | **A** | 常驻进程让数据库一直保持解锁，输出也更难解析；先用实测数据说话 |
| N9 | 审计粒度 | A. 只记失败与解锁事件 / B. 每次解析都记 | **A** | 每次连接已经有一条 `connect` 审计，再记会刷屏 |
| N10 | 顺手提炼共用样式（提示条、分段控件） | A. 本功能顺带做，并补 DESIGN.md / B. 不做，局部复制 | **B**，另起一条待办 | 避免扩大本功能的范围；提示条已有五六处各写各的，值得单独整理 |
| N11 | 1Password 状态探测若会触发授权 | A. 只探测版本，登录状态显示为「未知」 / B. 仍探测，接受弹框 | **A** | 打开设置页就弹 Windows Hello 会让用户困惑 |

---

## 17. 实施时再定的小问题

这些不影响阶段 0 开工，阶段 1 评审时拍板：

1. `UnlockCacheMinutes` 默认 15 分钟是否合适（上限暂定 480）。
2. 本地私钥文件的口令是否也支持从保险库取（§11）。
3. 1Password 多账户：账户选择放在设置页做全局默认，还是在选择器里按条目选（本文暂定：设置里设默认，选择器可以切换）。
4. KeePassXC 数据库别名改名时，要不要自动批量更新引用它的配置。
5. Bitwarden 解锁口令输错的重试上限（暂定 3 次）。
6. 条目选择器是否记住上次选的 provider 与数据库（放进本机设置文档）。

---

## 18. 修订记录

| 日期 | 版本 | 内容 |
|---|---|---|
| 2026-09-15 | v1 | 初稿：纠正前提、架构、分期；8 项决策定稿 |
| 2026-09-15 | v2 | 逐文件核实连接链路、设置与界面、装配与测试；新增三种载体五个构建点、连接入口全表、持久化点全表、内存驻留表；发现四处既有缺陷（§3）；按官方文档与源码核对三家 CLI（§8），修正「Bitwarden 约 1 秒」「KeePassXC 可按 UUID」等未核实表述；新增交互与非交互解析、仓储不变量、CLI 运行器规格、假 CLI 夹具、逐阶段任务清单、风险登记；新增 11 项待确认决策（§16） |
